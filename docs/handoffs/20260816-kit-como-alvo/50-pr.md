---
missao: 20260816-kit-como-alvo
fase: PR
status: done
pr_url: https://github.com/j0ruge/sdd_agents/pull/4
data: 2026-08-16 18:55
gate: "gh pr view https://github.com/j0ruge/sdd_agents/pull/4 --json url → ok"
---

# PR — quando o kit é o próprio alvo, quatro instrumentos param de afirmar o que nunca mediram

## O que esta fase fez

Checagens antes do push, todas confirmadas por comando nesta sessão:

- `git status --porcelain` → vazio (árvore limpa).
- `bash tests/run-all.sh` → verde, rc `0`, `score: 44 caught, 0 known gap(s), of 44` — o mesmo
  número que EXEC, QA, REVIEW e DOCS já haviam medido.
- `E2E_CMD=""` no `.sdd/config.sh` — não há E2E (o kit não tem interface).
- Branch corrente: `missao/20260816-kit-como-alvo`, diferente de `DEFAULT_BRANCH=main`.

`git push -u origin missao/20260816-kit-como-alvo` — sem conflito com a base, push direto.

`gh pr create --base main --head missao/20260816-kit-como-alvo` — corpo montado a partir de
`templates/pr-body.md`, preenchido com o que está nos handoffs de cada fase (nada inventado, nada
suavizado). O corpo enviado ao `gh` está preservado em
`docs/handoffs/20260816-kit-como-alvo/pr-body.md`.

## Resultado

PR **#4**, aberto contra `main`: <https://github.com/j0ruge/sdd_agents/pull/4>

Confirmado pelo `gh`, não só pelo texto desta sessão:

```
$ gh pr view https://github.com/j0ruge/sdd_agents/pull/4 --json url,number,title,state
{"number":4,"state":"OPEN","title":"fix(runner): quatro instrumentos param de afirmar o que não mediram","url":"https://github.com/j0ruge/sdd_agents/pull/4"}
```

## Próximo passo

Humano: revisar e mergear. Esta sessão não mergeia, não resolve conflito e não decide versão —
não havia `versao:` a decidir (`JIRA_ENABLED=false`).
