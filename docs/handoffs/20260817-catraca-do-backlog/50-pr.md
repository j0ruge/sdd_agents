---
missao: 20260817-catraca-do-backlog
fase: PR
status: done
pr_url: https://github.com/j0ruge/sdd_agents/pull/7
data: 2026-08-17 23:55
gate: "gh pr view https://github.com/j0ruge/sdd_agents/pull/7 --json url → ok"
---

# Handoff — PR — a catraca do backlog

> Escrito no fim de cada fase. É a última fase automatizada: depois dela só o merge, humano.

## TL;DR

Branch `missao/20260817-catraca-do-backlog` empurrada (`-u origin`, sem conflito com `main`) e PR
[#7](https://github.com/j0ruge/sdd_agents/pull/7) aberta contra `main`, com o corpo montado a
partir do que os handoffs das cinco fases anteriores já tinham escrito — nada inventado, nada
suavizado.

## Checagens antes do push

- `git status --porcelain` → vazio (árvore limpa).
- `tests/run-all.sh` (`TEST_CMD`) → rc 0, `suite green`, `score: 81 caught, 0 known gap(s), of 81`.
- `E2E_CMD` vazio em `.sdd/config.sh` — nada a rodar.
- Branch corrente `missao/20260817-catraca-do-backlog` ≠ `DEFAULT_BRANCH` (`main`).

## Push e PR

- `git push -u origin missao/20260817-catraca-do-backlog` — **sem conflito**, branch nova no
  remoto.
- `gh pr create --base main --head missao/20260817-catraca-do-backlog` — título
  `feat(health): o backlog ganha catraca contra crescimento silencioso`, seguindo a convenção do
  repo (`<tipo>(<escopo>): <o quê>`, confirmada contra os títulos das 6 PRs anteriores). Sem chave
  de issue — `JIRA_ENABLED=false`, fase TICKET pulada.
- Corpo montado a partir do template `templates/pr-body.md`, preenchido com o que está em
  `00-missao.md`, `01-plano.md`, `20-handoff-exec.md`, `30-handoff-qa.md`, `40-review-r1.md` e
  `45-docs.md`:
  - **Decisions for a Human:** a união das três pendências que se repetem em EXEC, QA e REVIEW
    (posse da linha `todo-findings` na baseline; o multiplicador do harness de mutação) mais a
    pendência do `.claude/napkin.md` que a fase DOCS registrou (barrada duas vezes pelo harness).
  - **Achados fora de escopo:** os 8 itens que `45-docs.md` confirma terem nascido nesta missão
    (68→76 no `TODO.md`), com âncora `arquivo:linha` cada.
  - **Riscos e não-feitos:** honesto sobre o custo da suíte (~3m30s a ~7min, era ~2m34s), os
    clusters 3/4/5 e o eixo de i18n que ficaram fora do escopo desde o `00-missao.md`, e que os 10
    itens fechados continuam contando até o merge (saem só na varredura pós-merge).

## Gate

`gh pr view https://github.com/j0ruge/sdd_agents/pull/7 --json url,number,state,title` →
`{"number":7,"state":"OPEN","title":"feat(health): o backlog ganha catraca contra crescimento
silencioso","url":"https://github.com/j0ruge/sdd_agents/pull/7"}`. A PR existe de verdade — o gate
não confia neste arquivo, confia no `gh`.

## Pendências / Decisions for a Human

> Já na PR; repetidas aqui só como ponteiro, não para bloquear nada.

- Posse da linha `todo-findings` na baseline (`tests/health-baseline.txt`, cabeçalho).
- O multiplicador do harness de mutação (`TODO.md`, "Custo e escala").
- `.claude/napkin.md` sem caminho de escrita por sessão headless (`TODO.md`, "Comentário e
  registro").

## Não-feito desta fase

- Nenhum merge. Nenhuma resolução de conflito (não houve conflito). Nenhum force push.
- Nenhuma versão decidida — `versao:` do `00-missao.md` está vazia e `JIRA_ENABLED=false`, então a
  âncora do rodapé do corpo da PR é `n/a` para os dois campos, como o template permite.
