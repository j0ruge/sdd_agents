# Notas de execução — o juiz não mente sobre a janela

> Append-only. O prompt de boot inlina as últimas 10 notas; a sessão não abre este arquivo.

- decision: I1(b) é **apagar com backup**, e não preservar-e-filtrar. Decisão humana de
  2026-09-11, tomada na aprovação do plano, respondendo à pergunta que o próprio I1 levanta:
  copie `~/.sdd/autonomy-log.jsonl` para `autonomy-log.jsonl.bak-2026-09-11` antes de remover as
  11 linhas sob `/tmp`, e mantenha o Check como está (`grep -c '"repo":"/tmp'` → `0`). Filtrar na
  leitura foi recusado por acrescentar uma regra que pode falhar aberta.
- decision: `BUDGET_MISSION_USD` fica em 150 sem ajuste. O humano optou por decidir na hora se a
  escalada `budget-exhausted` aparecer; não trate a parada como defeito do plano.
- 2026-09-11 22:15 · `I1` · **A metade (a) já estava entregue antes desta missão, e o plano não
  sabia.** A guarda que o I1 pede — o escritor recusar o ledger real a um checkout sob `/tmp` ou
  `$TMPDIR` — existe em `bin/sdd:2886` (`autonomy_append`, sobre `ledger_repo_is_temp`), veio com
  a ADR 0005 parte 3 em `28af7ea`, e já tem sensor: as **quatro** asserções `writer:` de
  `tests/check-autonomy.sh:3904` (dois pares diferenciais, A/B sob `/tmp` e C/D sob `/var/tmp`) e
  o mutante `mut_LEDGER_tmp_repo_allowed` (`tests/check-mutation.sh:903`). Nada foi reescrito: o
  probe vermelho que o plano manda escrever **não pode** ficar vermelho, porque o mundo em que o
  fixture contamina já está fechado. É o caso que a cláusula Jidoka do I1 previu, com a resposta
  invertida — o mundo não foi impossível de construir, foi impossível de **quebrar**. `SDD_MUTANT`
  não existe no runner e não foi criado: o catálogo sabota uma cópia em `mktemp -d`, que a guarda
  de caminho já recusa.
- 2026-09-11 22:15 · `I1` · Metade (b) executada como o humano decidiu — apagar com backup.
  `~/.sdd/autonomy-log.jsonl` 292 → 281 linhas; as 11 sob `/tmp` (`/tmp/qa-portas/clone` 5,
  `/tmp/sddrev.q49jXD/r7b` 3, três `/tmp/manual-*` 1 cada) saíram; sobraram só os dois repos reais
  (`sdd_agents` 156, `sales_quote` 125). Original em `~/.sdd/autonomy-log.jsonl.bak-2026-09-11`,
  com a grafia de data que a decisão humana pediu. Filtrado por `grep -v` e não por `jq`, de
  propósito: `jq` reserializaria as 281 linhas sobreviventes (ordem de chave, formato de número) e
  o diff seria indistinguível de uma reescrita — provado por
  `grep -v … bak | diff -q - autonomy-log.jsonl` → idêntico. Check verde: `0`.
- 2026-09-11 23:40 · `I2` · **A rota que o achado nomeava estava MORTA; a divergência, viva por outra porta.** O `TODO.md:815` dizia "com a closure como primeira linha de um sha, os dois respondem `latest`/`previous` INVERTIDOS". Reproduzido nesta sessão: **não invertem** — o `shas_in_file_order` positivo de `20260831` já fechou a rota do `gate_pass` nos dois lados. O que estava vivo era a linha **KAIZEN** liderando uma versão: o `kaizen_series` tira as sessões do próprio juiz do eixo (`$meta`), o `cmd_autonomy` não tinha essa separação, e a tabela leu `bbbbbbb` contra `ccccccc` da série sobre um arquivo só. Escritor real (`cmd_kaizen`), não linha editada à mão — e no repo do kit a sessão do juiz pousa num sha só dela por construção. Lição para quem lê achado de revisão: **o defeito pode sobreviver ao mundo que o achado descreve**; o probe diferencial acha a porta, a prosa do achado não.
- 2026-09-11 23:40 · `I2` · A metade (b) (`missions` sobre linha não-graduável) estava viva **exatamente como escrita**: `missions: 2` sobre `detail: 1`, e a `composition` da ADR 0005 junto. Fechada com `graded_row` — UMA definição para os três leitores, escrita positivamente.
- 2026-09-11 23:40 · `I2` · ⚠️ **Apostrofe em comentário DENTRO do programa jq mata o `bin/sdd` na hora do parse.** Os dois blocos de jq do `cmd_autonomy`/`kaizen_series` são uma string shell de aspas simples só; escrevi "the judge's own session" no comentário **e** na frase impressa e o `bash -n` morreu a 500 linhas de distância. O aviso já está nos comentários do próprio bloco — custou duas voltas mesmo assim. Vale para a frase que o runner IMPRIME também, e o probe teve de aprender a grafia sem apostrofe.
- 2026-09-11 23:40 · `I2` · Dois commits: `7e6b3f5` (conserto + probes + 2 mutantes) e `d93b9bd` (os dois achados marcados `RESOLVIDO por 7e6b3f5`). A **catraca não se move** (105): item com `RESOLVIDO por` segue aberto até o merge — quem a move é o I6. Cada item foi encurtado para o teto de 8 linhas do `check-todo.sh` no mesmo diff; o que saiu foi a linha `Direção:`, que achado fechado não tem mais.
- 2026-09-12 00:40 · `I3` · **Uma decisão de população fechou os dois achados, e cada um exigiu uma ligação PRÓPRIA em vez de uma população borrada.** `reopened` ganhou `$every_row` (sessões + closures `gate_pass`); `$every_session` ficou intacto porque `launches` (run_id distinto) e `$history_extra` (número de tela) significam outra coisa. O laço de revisão foi inteiro para `$every` — fronteira, numerador **e** denominador: numerador sobre uma população e denominador sobre outra faz a célula ler 160%, e eu quase escrevi isso.
- 2026-09-12 00:40 · `I3` · ⚠️ **A âncora de um mutante existente apodreceu no meu diff, e `bash -n` não acusa.** `mut_AUTONOMY_reopened_ignores_gate` ancorava em `$r.gate == "pass" then .maxpass`, texto que meu conserto reescreveu — o catálogo é opt-in, então a suíte rápida teria ficado verde com o mutante morto (a classe que o `c962e2e` fechou com o carimbo). Verifiquei os **seis** mutantes da vizinhança um a um numa sandbox à mão (sem rodar o catálogo inteiro), todos mordem. Conserto que toca linha ancorada por mutante confere as âncoras no mesmo diff.
- 2026-09-12 00:40 · `I3` · ⚠️ **Apóstrofe em comentário dentro do bloco jq mata o `bin/sdd`, de novo** — escrevi "the mission's whole local history" e o `bash -n` morreu 400 linhas adiante. O aviso já está nas notas do I2 e nos comentários do próprio bloco; custou uma volta mesmo assim. Segundo tropeço da sessão, no mesmo par de blocos.
- 2026-09-12 00:40 · `I3` · ⚠️ **O `localize` reescreve o campo `repo` do fixture, e um `sed` de gêmeo ancorado no literal `/p1` não casa nada** — o resultado são dois arquivos idênticos e um diferencial verde comparando um arquivo consigo mesmo. Quem pegou foi o piso (`o gêmeo difere em exatamente duas linhas`), que é justamente a regra do CLAUDE.md sobre probe que prova primeiro ter sabotado o que dizia sabotar. Ancore no `event`/`phase`, nunca no `repo`.
- 2026-09-12 00:40 · `I3` · Medido no ledger real, `--all-repos --by-mission`: `20260818-lote-facil` 28% → **62%**, `20260816-portas-do-humano` 24% → **42%**, `20260816-kit-como-alvo` 13% → **31%**, `20260902-o-rascunho-legado-fala-cru` 0 → **6 reopened**, `20260907-o-descarte-do-pagamento-fala` 0 → **7 reopened**. O número sobe porque o instrumento parou de sub-reportar: é o conserto, não regressão. Material do `KAIZEN_LOG` no I6.
- 2026-09-12 00:40 · `I3` · A frase de contabilidade passou a nomear o laço (`launches, reopened e o review loop …`), em três lugares no mesmo diff — `bin/sdd`, o probe que a grepa e `docs/pipeline.md`. ⚠️ O `check-lang.sh` reprova o **slug da própria missão** citado no `docs/pipeline.md`: slug PT-BR em doc de superfície é prosa portuguesa para o sensor. Cite a data, não o slug.
- 2026-09-12 00:40 · `I3` · Dois commits: `392f526` (conserto + 6 probes + 3 mutantes novos + 1 âncora consertada) e `47113c7` (os dois achados marcados `RESOLVIDO por 392f526`). Catraca parada em 105 de propósito — quem a move é o I6.
- 2026-09-12 01:40 · `I4` · **Quarto evento, e ele re-derivou o contrato em vez de herdá-lo.** O `close` não é linha de sessão vestindo `phase: "CLOSE"` — CLOSE não é fase, não tem gate, o `current_phase()` nunca a deriva, e `moved`/`gate` zerados entrariam na aritmética do juiz como fase graduada que rodou e não disse nada. Mesma recusa que o `gate_pass` já fazia.
- 2026-09-12 01:40 · `I4` · ⚠️ **O comentário do `graded_row` previu esta sessão por escrito** — *"a forma negativa faz um QUARTO evento contar como missão por omissão no dia em que alguém o acrescentar"* — e por ele estar escrito positivamente o `close` não cunhou versão nem contou missão. Predicado de admissão positivo pagou juros um mês depois de ser escrito; o `is_unrecognized` e o `$all` dos dois leitores, esses, precisaram aprender o evento à mão no mesmo commit.
- 2026-09-12 01:40 · `I4` · **A asserção do `close` é DIFERENCIAL porque o comando tem dois braços e só um compra sessão.** O braço que acha o issue já `Done` volta antes de lançar o `claude`. Um writer no topo da função — o conserto óbvio e errado — gravaria sessão que ninguém gastou. Cada metade sozinha passa verde; o piso da segunda é `sessions:0`, senão "nenhuma linha" também é a resposta de um mundo onde o close nem rodou.
- 2026-09-12 01:40 · `I4` · **Consertar uma porta partiu dois probes vizinhos, e o modo de falha é instrutivo:** `rows '.moved'` e `jq -s length` passaram a ler DUAS linhas onde liam uma. Nenhum estava errado antes; os dois afirmavam sobre o ARQUIVO uma propriedade que era da linha de sessão. Escopei ambos em `select(.event == "session")` com o porquê no comentário. Linha nova no ledger é mudança de população para todo probe que conta sem filtrar.
- 2026-09-12 01:40 · `I4` · ⚠️ **Verifiquei os dois mutantes novos numa sandbox à mão antes de commitar** (o catálogo é opt-in, a suíte rápida não o roda). Ambos mordem com 2 FAILs. Usei a lição do I3 ao contrário: o probe do mutante checa primeiro que o `sed` MUDOU o arquivo (`diff -q` ⇒ `ANCHOR ROTTEN`), senão "sabotei e a suíte caiu" seria conclusão de sabotagem vazia.
- 2026-09-12 01:40 · `I4` · Dois commits: `aa3c0a2` (conserto + 6 probes + 2 mutantes + `docs/pipeline.md` e `CONTEXT.md` no mesmo diff, que é o contrato de forma-de-linha) e `659acf0` (os dois achados marcados `RESOLVIDO por aa3c0a2`, encurtados ao teto de 8 linhas). Catraca parada em 105 de propósito — quem a move é o I6.
- 2026-09-12 03:05 · `I5` · **Só o harness veta; a janela NÃO.** O plano manda confirmar que a fatia real segue `sufficient: true`, e a confirmação decide o desenho: `window_broken` tinha de ser instrumento publicado e não cláusula de `sufficient`, senão a mudança reprovaria o veredito já escrito — gate insatisfazível, a classe que o princípio 1 proíbe. Medido no recorte real da janela 4 (`head -280` do ledger): `sufficient: true`, `harness: ["2.1.263"]`, `window_broken: true` com **3 missões encalhadas** — exatamente a ruptura que só um humano lendo `git log` via.
- 2026-09-12 03:05 · `I5` · ⚠️ **O quinto mutante foi RECUSADO, e a recusa é a regra do CLAUDE.md sendo obedecida ao pé da letra.** `mut_KAIZEN_guard_harness_negative` trocava `($n == 0 or $n == 1)` por `($n > 1 | not)` — sobre os inteiros as duas grafias são a MESMA função, então nenhum probe pode matá-lo. Em vez de deixar decoração no catálogo ou apagar a regra por "inquebrável", declarei no comentário **qual mundo não consegui construir** (uma fatia cuja lista de harness as duas grafias respondem diferente) e o que a forma positiva compra quando o conjunto aceito deixar de ser um comprimento. D15: dívida declarada é limite.
- 2026-09-12 03:05 · `I5` · A abertura da janela é a **última linha `KAIZEN`** do ledger — onde o veredito anterior fechou —, não o começo do arquivo. O par diferencial é o que prova isso: a mesma missão numa versão antiga, de um lado e do outro da linha `KAIZEN`. Implementação que lê do começo do arquivo responde `true 1` nos dois e passaria por sorte no primeiro; quem a mata é `mut_KAIZEN_window_ignores_verdict`.
- 2026-09-12 03:05 · `I5` · Os quatro mutantes foram verificados à mão em sandbox (o catálogo é opt-in), cada um checando antes que o `sed` mudou o arquivo (`ANCHOR ROTTEN`). ⚠️ A primeira sandbox era **incompleta** (`cp -r bin tests …`, sem `docs/adr`) e devolveu 25 FAILs para todo mutante — número alto demais é sinal de sandbox quebrada, não de mutante forte. Com `git archive HEAD` cada um passou a ser pego por **exatamente** a asserção que nomeia o defeito dele.
- 2026-09-12 03:05 · `I5` · Dois commits: `5e3c427` (conserto + 6 probes + 4 mutantes + `docs/pipeline.md` e `CONTEXT.md` no mesmo diff, que é o contrato da forma da série) e `a5d99a0` (os dois achados `RESOLVIDO por 5e3c427`, com o item da janela encurtado ao teto de 8 linhas). Catraca parada em 105 de propósito — quem a move é o I6. ⚠️ `tests/check-mutation.sh --list` **não é flag**: ele ignora e roda o catálogo inteiro (>10 min). Para contar mutantes use `grep -c '^mut_'`.
- 2026-09-12 04:30 · `I6` · **O décimo item saiu do corte, e o critério foi a DIREÇÃO da falha, não o tamanho.** O plano deixava a escolha ao executor e autorizava fechar em 9. Escolhi o censo `guard:` que conta captura escrita dentro de comentário (`check-health.sh`) porque o próprio item dizia *"falha FECHADA, então não certifica nada de errado"* — é exatamente a metade da D15 que separa limite de fail-open. Os candidatos que recusei foram os dois de custo da suíte (alvo "<30 s" da D7): não são fail-open nem têm consumidor externo, mas são **decisão humana pendente** e não limite de sensor; mover decisão de humano para cabeçalho de teste é perdê-la, não declará-la.
- 2026-09-12 04:30 · `I6` · ⚠️ **O sensor de idioma reprovou o cabeçalho que eu acabara de escrever, e o defeito era um achado ainda ABERTO do próprio arquivo.** Citei o slug pt-BR da missão que mediu o defeito (`20260819-fecho-que-nao-mente`) dentro de `tests/check-health.sh`, que é superfície inglesa — é o item *"Slug de missão em pt-BR não pode ser citado na superfície inglesa"*, que permanece nos 95. Reescrevi com a data e uma frase dizendo que o sensor é dono daquela superfície. Lição: mover dívida para o cabeçalho de um sensor sujeita a prosa às regras que esse arquivo já obedece — o cabeçalho é código, não nota de rodapé.
- 2026-09-12 04:30 · `I6` · **Apagar os dez deixou um heading mentindo.** A seção `### Adiados por YAGNI` ficou com zero adiamentos e doze achados que missões recentes apendaram ao fim do arquivo sem classificar. Renomeei dizendo isso, com o ponteiro para Y1–Y3 do `CONTEXT.md`: heading que descreve conteúdo que não tem é a mesma classe dos dez itens que este incremento moveu (`"o cabeçalho diz dez, o comando responde 13"`), e deixá-lo seria fechar a classe com uma instância nova aberta no mesmo diff.
- 2026-09-12 04:30 · `I6` · Os três YAGNI ganharam no `CONTEXT.md` uma coluna que não existia no `TODO.md`: **o evento que reabre**. Adiamento sem gatilho escrito é um item que ninguém volta a ler — Y2 (`git worktree`) carrega ainda o aviso de que não é `git worktree add` no laço, porque o ledger carimba caminho e um worktree já confundiu a identidade do repo.
- 2026-09-12 04:30 · `I6` · Commit `4d9b7b8`, suíte verde, `105 → 95` achados e `todo-findings 95` na catraca. ⚠️ **Este era o ÚLTIMO incremento, e `tests/health-baseline.txt` está dentro da chave do carimbo de mutação:** o `./bin/sdd health` (20–50 min) tem de rodar AGORA, depois deste commit de código, e o `gate_PR` exige o carimbo. Registrar qualquer achado novo no `TODO.md` depois dele mata o carimbo de novo — é a colisão que o próprio backlog registra.
- 2026-09-12 06:40 · `I6` · 🛑 **JIDOKA — a linha PARA, e não é o I6 que a parou.** O I6 fechou verde (`4d9b7b8`, `ok 95 finding(s)`, suíte verde) e o plano manda rodar `./bin/sdd health` depois dele, porque o `tests/health-baseline.txt` está dentro da chave do carimbo de mutação. O `sdd health` voltou **vermelho**, e o defeito é de incrementos ANTERIORES: `score: 286 caught, 0 known gap(s), of 289` mais `the catalogue ran 289 of the 293 mutant(s) tests/check-mutation.sh defines`. Marquei I2, I4 e I5 `blocked` e parei. Não consertei: a regra desta fase é que vermelho de incremento anterior vira `blocked` + parada, e o `gate_PR` exige o carimbo que não existe.
- 2026-09-12 06:40 · `I6` · **Defeito A — três âncoras de mutante apodrecidas, cada uma com dono provado por `git log -S`.** `CATALOGUE-BROKEN: KAIZEN_composition_session_unit` ("the mutation did not apply — did the anchor change in bin/sdd?"): o `sed` procura `composition: ($rows | group_by(.repo // "")` e quem mexeu foi o **I2** (`7e6b3f5`). `CATALOGUE-BROKEN: LEDGER_gate_pass_unrecognized` e `LEDGER_gate_pass_not_admitted`: os dois `sed` procuram a forma `… or is_gate_pass)` e o **I4** (`aa3c0a2`) a reescreveu para `… or is_gate_pass or is_close)` ao ensinar o quarto evento aos dois leitores. Mutante com âncora podre reporta `CATALOGUE-BROKEN`, não `caught`: é o achado nº 1 do `TODO.md` acontecendo (*"âncora morta de mutante só aparece no catálogo inteiro"*).
- 2026-09-12 06:40 · `I6` · **Defeito B — quatro mutantes DEFINIDOS e nunca REGISTRADOS, e o `of N` não os conta.** `grep -c '^mut_' tests/check-mutation.sh` responde **293**; o `CATALOG=(` tem **289**. O comm das duas listas nomeia exatamente os quatro do I5: `mut_KAIZEN_guard_harness_blind`, `mut_KAIZEN_guard_why_silent`, `mut_KAIZEN_window_break_blind`, `mut_KAIZEN_window_ignores_verdict`. A nota do I5 diz que eles foram verificados **à mão em sandbox** — e é essa a lacuna: sandbox à mão prova o mutante, nunca o **registro** dele. O `score:` nunca reclama sozinho, porque `caught == of` continua verdadeiro sobre a lista encolhida; quem pegou foi a asserção de piso do `sdd health`, que compara o que **rodou** com o que está **definido**.
- 2026-09-12 06:40 · `I6` · **A lição é uma só e vale para as três: o catálogo ser opt-in transfere para o incremento o dever de rodá-lo, e três incrementos seguidos não rodaram.** I2, I4 e I5 acrescentaram 12 mutantes e verificaram cada um numa sandbox; nenhum rodou o catálogo inteiro, que é a única passada que enxerga (a) âncora que o próprio conserto apodreceu e (b) mutante fora do `CATALOG`. Verificação por mutante e verificação do catálogo respondem perguntas diferentes — a primeira diz "este mutante morde", a segunda diz "a lista ainda é a lista". Direção para quem desbloquear: re-ancorar os três `sed` na forma nova, acrescentar os quatro nomes ao `CATALOG=(`, e só então `./bin/sdd health` (20–50 min) para escrever o carimbo.
- 2026-09-12 06:40 · `I6` · ⚠️ **Dois carimbos de tempo que custam sessão e não estão em lugar nenhum:** `tail -25` num `sdd health` **come a lista de mutantes com problema** — os `fail` saem em stderr no meio da saída, e eu precisei rodar o catálogo uma segunda vez (≈50 min) só para ler três nomes; redirecione para arquivo e grepe. E `pgrep -f check-mutation.sh` **casa o próprio shell de espera** (`until ! pgrep …` num `zsh -c` cujo argv contém o padrão), então o laço nunca sai: espere pelo PID, nunca pelo padrão.
- 2026-09-12 09:35 · `I2/I4/I5` · **Jidoka destravada por intervenção humana, e o conserto foi mecânico.** Os dois defeitos que o I6 reportou foram fechados em `e748c97`: (a) as três âncoras podres re-ancoradas na forma nova — `composition: ($graded |` para o `KAIZEN_composition_session_unit`, e `… or is_gate_pass or is_close)` para os dois `LEDGER_gate_pass_*`; (b) os quatro mutantes do I5 acrescentados ao `CATALOG=(` junto do irmão `KAIZEN_guard_floor_unpublished`, fechando 289 → 293 contra os 293 definidos.
- 2026-09-12 09:35 · `I2/I4/I5` · Os sete foram verificados numa sandbox de `git archive HEAD`, cada um provando **primeiro** que o `sed` mudou o arquivo (senão a conclusão seria de sabotagem vazia): re-ancorados mordem com 5, 3 e 8 FAILs; os quatro novos com 1 cada; controle íntegro verde. Depois `./bin/sdd health` (40 min): `score: 293 caught, 0 known gap(s), of 293`, carimbo `17591a682fecf870219142b1ee8438d5` escrito, `kit healthy`.
- 2026-09-12 09:35 · `I2/I4/I5` · ⚠️ **A lição do I6 está confirmada e é do DESENHO, não dos incrementos.** Verificar um mutante em sandbox responde "este morde"; rodar o catálogo responde "a lista ainda é a lista". Como o catálogo é opt-in desde `4c86712`, a segunda pergunta só é feita por quem lembrar de fazê-la — e três incrementos seguidos não lembraram. Vale notar que esta classe **só existe quando o alvo é o próprio kit**: em repo-alvo comum o `has_mutation_catalogue` responde falso e o `gate_PR` não pede carimbo.
- 2026-09-12 09:35 · `I2/I4/I5` · ⚠️ **Não registre achado novo no `TODO.md` a partir daqui sem re-carimbar:** `tests/health-baseline.txt` está dentro da chave do carimbo, e o `gate_PR` exige o carimbo. A colisão já está no backlog.
- 2026-09-12 09:51 · `QA` · **QA fecha sem achado: 0 spec nova, 0 `F<n>`.** Projeto sem interface — andei as 4 jornadas de CLI à mão (série do juiz, guarda de harness em fixture com `SDD_STATE_DIR`, `--by-mission` sobre o ledger real, catraca do backlog) e re-executei os 6 Checks do checkpoint; tudo verde, evidência no `gate:` do `30-handoff-qa.md`. A métrica 3 foi **reproduzida**: uma linha `event:"session"` com `harness:"9.9.9"` num recorte do ledger vira `sufficient: false, why: ["harness_mixed"]`. ⚠️ Dois carimbos para a REVIEW: `./bin/sdd autonomy --series` NÃO existe (é `kaizen --series`, e o boot do handoff de EXEC erra isso), e o carimbo de mutação `17591a68…` segue válido — tocar `bin/ tests/ templates/ config/` (ou o `TODO.md`, via `health-baseline.txt`) custa 20–50 min de `sdd health`.
- 2026-09-12 10:08 · `REVIEW r1` · **Rodada 1 fecha em B com 12 achados (4 HIGH, 3 MEDIUM, 5 LOW) e cinco `R<n>`.** As três métricas da missão foram re-verificadas e estão cumpridas — o que reprova é o complemento. `R1`/`R2`: a linha `event:"close"` (I4) e a exclusão `$meta` (KAIZEN) entram no total do cabeçalho de `sdd autonomy` e não caem em bucket nenhum — reproduzido com controle negativo (`header=1 sum=1 OK` → `header=2 sum=1 DRIFT` nos dois casos), e o `assert_bucket_sum` do `check-autonomy.sh` reprovaria se algum fixture seu carregasse essas linhas (`grep -c '"event":"close"' tests/check-autonomy.sh` → `0`). `R3`: `window_missions_stranded` é uma subtração de contagens, então missão que atravessa o sha conta dos dois lados e some — `{"stranded":0,"broken":false}` sobre uma missão com `EXEC` em `aaaaaaa` e `QA` no sha julgado. `R4`: `git diff main...HEAD --name-only -- agents/` é vazio; os quatro campos novos da guarda não têm leitor. `R5`: lote dos #6–#9.
- 2026-09-12 10:08 · `REVIEW r1` · ⚠️ **Dois carimbos para quem pegar os `R<n>`.** (1) Todo `R<n>` toca `bin/` ou `tests/` e **mata o carimbo** `17591a68…` que o `gate_PR` exige: `./bin/sdd health` (20–50 min) roda depois do **último** commit de código de R1–R5, nunca entre eles — é a mesma armadilha que já parou a linha nesta missão no I6. (2) Nenhum achado foi para o `TODO.md` de propósito: item novo lá move `tests/health-baseline.txt`, que está dentro da chave do carimbo, e pagaria o `sdd health` duas vezes. O `R4` sincroniza o espelho com `sdd install --force`, nunca `cp` nem Edit.
- 2026-09-12 10:08 · `REVIEW r1` · **Uma pendência humana, sem `R<n>` de propósito: `harness_mixed` veta `guard.sufficient` para sempre.** Reproduzido: piso batido com 3 missões e depois com 8, e as duas respondem `sufficient:false, why:["harness_mixed"]` — `$harness` é `unique` sobre a fatia, que é presa ao `kit_sha`, então nenhuma missão nova cura. `gate_KAIZEN` só aceita `indeterminado` nesse estado, logo a versão nunca é graduada. O gate continua **satisfazível** (o veredito `indeterminado` fecha), então não é a classe do princípio 1. Não escrevi incremento porque o veto É a métrica 3 que o humano aprovou em `aprovacao: humano-2026-09-11`, e reverter decisão aprovada por conta própria seria concordar performaticamente com a própria crítica. Três saídas registradas no relatório; a assimetria com `degenerate_axis` e `window_broken` (nenhum dos dois veta) é o argumento que falta.
- 2026-09-12 10:23 · `R1` · **Sexto bucket, e a lição é a do quinto repetida um evento depois.** `is_close` foi admitido no enum (deixou de ser `unrecognized`) sem ser nomeado em lugar nenhum onde a aritmética fecha: o total do cabeçalho é a variável de shell `total=`, sobre `ledger_row_is_local`, que não conhece filtro de dentro do programa grande. Vermelho observado antes do conserto sobre fixture de duas linhas — header 2 contra buckets 1. Conserto: `$closes` + frase própria (não é exclusão: a linha é local e está no cabeçalho, só não gradua fase), e `assert_bucket_sum` passou a somar seis termos. Mutante `LEDGER_close_bucket_unnamed` verificado em sandbox (a sabotagem provou primeiro que mudou o arquivo) — morde com as duas asserções nomeadas e só elas. Commit `1563bc8`, suíte verde.
- 2026-09-12 10:23 · `R1` · ⚠️ **Dois carimbos para quem pegar R2–R5.** (1) O `check-lang.sh` reprova comentário de sensor que cite o **slug da missão** (`20260911-o-juiz-nao-mente-sobre-a-janela` é português na superfície inglesa do kit) — custou uma volta aqui; escreva "the 2026-09-11 judge mission". (2) O `sdd health` **não** foi rodado de propósito: o carimbo `17591a68…` está morto desde este commit e só vale a pena re-carimbar **depois do último commit de código de R1–R5** (20–50 min), como a r1 já avisou. Nada foi para o `TODO.md`, pelo mesmo motivo.
- 2026-09-12 11:05 · `R2` · **Sétimo bucket, e desta vez o defeito tinha DUAS metades com frases diferentes.** O `$meta` (linha `phase == "KAIZEN"`) sai do eixo antes dos buckets, mas é LOCAL — logo já estava no `total=` do shell. (a) A frase passou de `$outside` para `$inside`, com redação que diz o que o código faz ("sai do EIXO"), e virou o sétimo termo do `assert_bucket_sum`; (b) `$local_total` soma `$meta` de volta, senão a linha "never part of the N counted above" cita um número que o cabeçalho nunca imprimiu — sobre três linhas dizia "never part of the 1" sob header "2 row(s)". Vermelho observado antes (header 2, buckets 1). Commit `a26d489`, suíte verde.
- 2026-09-12 11:05 · `R2` · ⚠️ **O comentário ao lado do split AFIRMAVA o oposto do que o código fazia** — "these rows were never in the header total". É a classe que o `CLAUDE.md` já nomeia ("comentário que afirma paridade não é paridade"), e foi por isso que a propriedade foi escrita como asserção e o comentário reescrito no mesmo commit. Dois mutantes (`LEDGER_meta_bucket_unnamed`, `LEDGER_meta_off_the_local_total`), um por metade, verificados em sandbox provando primeiro que o `sed` mudou o arquivo; catálogo 296 definidos contra 296 listados.
- 2026-09-12 11:05 · `R2` · ⚠️ **Mudar a redação de uma frase impressa move todo probe que a grepa — a lição do I6 outra vez.** O `check-kaizen.sh` tinha um piso anti-vacuidade ancorado em `row(s) excluded: written by the judge`; a suíte inteira ficou vermelha nessa única asserção e ela foi re-ancorada na frase nova. Foi também o que fez o comentário do mutante A dizer "três asserções, dois sensores" em vez do "e nada mais" que eu ia escrever de memória. Para quem pega R3–R5: o carimbo `17591a68…` segue morto, e o `sdd health` roda **depois do último commit de código**, nunca entre eles.
