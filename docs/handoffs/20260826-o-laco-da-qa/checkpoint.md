---
missao: 20260826-o-laco-da-qa
atualizado: 2026-08-26 17:50
---

# Checkpoint — a fase QA para de girar em bug que ninguém pode fechar

> **Este arquivo é lido por máquina.** O runner faz parse da tabela abaixo para decidir a
> próxima fase. Não mude as colunas, não mude os tokens de status, não quebre linhas dentro de
> uma célula. Detalhe narrativo vai no `01-plano.md`, não aqui.
>
> Atualizar o checkpoint é o **último ato** de cada incremento — depois do commit, nunca antes.
> Status válidos: `pending` · `doing` · `done` · `blocked`.

| ID | Incremento | Check (comando → esperado) | Status | Commit |
|---|---|---|---|---|
| I1 | o arquivo de bug declara quem pode fechá-lo | `grep -c 'Closable by:' docs/qa/templates/bug.md` → `1` (o comentário de enum na forma dos outros campos está descrito no 01-plano.md, I1) | done | 525b216 |
| I2 | a Âncora 3 distingue gênero, e o desconhecido barra | `o=$(bash tests/check-gates.sh 2>&1); grep -c '^  ok    ' <<< "$o"` → 3 a mais que antes; e a asserção **diferencial** (gênero humano passa × gênero agente reprova, saídas comparadas entre si) entre os `ok` | done | 0a0f760 |
| I3 | `status: blocked` escala na primeira sessão | `o=$(bash tests/check-autonomy.sh 2>&1); grep -c '^  ok    ' <<< "$o"` → 1 a mais; o regime novo prova **uma** sessão e rc 3, onde hoje são duas | done | 4c9b080 |
| I4 | o contrato do `sdd-qa` para de mentir | depois de `./bin/sdd install --force`: `cmp -s agents/sdd-qa.md .claude/agents/sdd-qa.md; echo espelho=$?` → `espelho=0` (byte a byte, medido), e `grep -c 'do not block the pipeline' agents/sdd-qa.md` → `0` | done | cf43bb3 |
| F1 | a Âncora 3 lê o gênero do CAMPO e como palavra INTEIRA: hoje ela casa por prefixo (`humano`, o pt-BR, passa) e em qualquer linha do arquivo (um bug `agent` que só CITA a linha humana passa) | `bash tests/check-gates.sh` → rc 0 com as linhas `the genre is the whole word: 'human' passes, the near-miss 'humano' still blocks` e `the genre is the FIELD: a bug that merely quotes the human line still blocks` entre as `^  ok    `; e re-walk da jornada do gênero verde nas 3 asserções antigas; e `./tests/run-all.sh` → `suite green`, rc 0 | done | 878b37c |
| F2 | a escalada imediata de `status: blocked` passa a valer também no retry inline (`bin/sdd:3538-3554`): hoje o marcador SOBREVIVE à volta e escalona a fase seguinte com motivo falso e `kind` errado no ledger | `bash tests/check-autonomy.sh` → rc 0 com as linhas `a retry session that declares blocked escalates on that declaration, not a lap later` e `the escalation names the phase whose handoff declared it, never the one that came next` entre as `^  ok    `; e re-walk da jornada de escalada verde nas duas metades de cada diferencial; e `./tests/run-all.sh` → `suite green`, rc 0 | done | 26b74a0 |

## Notas de execução

> Uma linha por evento relevante: bloqueio, decisão tomada, desvio do plano com justificativa.
> É o que a próxima sessão lê para não repetir um erro que já custou caro.

- 2026-08-26 13:06 · `PLAN` · plano fechado com o humano presente. A raiz é maior que os dois achados diziam: `agents/sdd-qa.md:142` **proíbe** qualquer agente de escrever `Status:` — o dono é a skill. Por isso bug de gênero humano é insatisfazível por construção, e por isso a opção C (gênero) e não a B (dar o poder ao agente).
- 2026-08-26 13:06 · `PLAN` · ⚠️ o registry do PRÓPRIO kit está limpo (6 bugs, 0 `open`), então o regime que esta missão conserta **não é reproduzível** pelo registry real — os testes constroem o registry em fixture.
- 2026-08-26 15:40 · `EXEC` · I1 `done` em `525b216`. Baseline antes de tocar em nada: `tests/run-all.sh` → `suite green`, rc 0. Red observado antes da edição: `grep -c 'Closable by:' docs/qa/templates/bug.md` → `0`; depois → `1`. Suíte depois: `suite green`, rc 0, 656 asserções, 0 FAIL.
- 2026-08-26 15:40 · `EXEC` · ⚠️ **Leia antes de escrever a âncora do I2.** A forma escolhida foi `- **Closable by:** agent <!-- agent | human -->`, a mesma do `Status:` — valor **colado** ao nome do campo, enum no comentário. Motivo medido: os arquivos reais do registry **preservam** o comentário de enum (`grep -h '^- \*\*Status:\*\*' docs/qa/bugs/*.md` → 6 de 6 com a legenda), então a palavra `human` vai existir em **todo** arquivo de bug, dentro do comentário. Uma âncora `.*human` falha aberta e desliga a Âncora 3 para o registry inteiro — que é exatamente a armadilha já registrada em `bin/sdd:587-588` para o `closed`. Ancore em `^\-[[:space:]]+\*\*Closable by:\*\*[[:space:]]+human`; medido contra o template novo, que responde `0`.
- 2026-08-26 15:40 · `EXEC` · Desvio de forma com justificativa: o `01-plano.md` grafa o valor como `<agent | human>`; foi implementado como o default concreto `agent`. A assimetria é contrato — marcar `human` exige procedência citada em `arquivo:linha` (I4), e `agent` é o que vale até alguém provar o contrário. Os dois fecham igual sob âncora estrita (arquivo não triado barra, decisão 3 do grill); o default concreto ganha por simetria com o campo irmão que a Âncora 3 já lê.
- 2026-08-26 15:40 · `EXEC` · Para a fase `DOCS` (drift desta missão, não é achado fora de escopo): `docs/qa/README.md:109-113` descreve a Âncora 3 como "counts files … matching `- **Status:** open` and refuses the phase while any exist", que o I2 torna falso. O mesmo parágrafo já cita `bin/sdd:485-491`, e hoje esse trecho é prosa do `gate_TICKET` — a Âncora 3 está em `bin/sdd:598`. Re-ancorar junto com a reescrita.
- 2026-08-26 17:05 · `EXEC` · I2 `done` em `0a0f760`. Baseline antes de tocar em nada: `tests/run-all.sh` → `suite green`, rc 0, 656 asserções. Depois: `suite green`, rc 0, **659** asserções, 0 FAIL. `check-gates.sh`: **129 → 132** `ok`, exatamente os 3 do Check.
- 2026-08-26 17:05 · `EXEC` · Red observado antes da implementação e **pelo motivo certo**, não só vermelho: o diferencial respondeu `QA|QA|same` (esperado `REVIEW|QA|differ`) e o par de marcadores respondeu `1|1` (esperado `0|1`) — ou seja, o regime humano barrava exatamente como o de agente, que é o defeito. A terceira asserção (fail-safe, campo ausente) já passava no vermelho, de propósito: ela não mede o defeito, mede o conserto não ir longe demais.
- 2026-08-26 17:05 · `EXEC` · ⚠️ **Lição do mutante, para quem escrever o do I3.** A primeira forma do `mut_QA_bug_genre_ignored` ancorava em `.*Closable by.*human.*` e teria casado **duas linhas de comentário** do `bin/sdd` além da linha de código — comentário que carrega as mesmas palavras da regra que descreve. Um mutante que reescreve só comentário **aplica** (passa da guarda `cmp -s`, rc 90) e não sabota nada: entraria no catálogo certificando uma proteção que ninguém mede, que é a classe já registrada em `mut_LEDGER_repo_root_cdpath_leak`. A âncora final é a linha de CÓDIGO inteira (`^    if grep -qE .*Closable by.*then continue; fi$`). Verificado à mão numa sandbox antes de commitar: aplica em exatamente 1 linha, `bash -n` verde, e `check-gates.sh` morre com `QA|QA|same`.
- 2026-08-26 17:05 · `EXEC` · Para o I3 — o que já está lido no arquivo, para não re-descobrir: `bin/sdd:564` publica `GATE_WHY="30-handoff-qa.md has status: blocked"` quando o handoff declara `blocked`, e é o **único** ponto do `gate_QA` que usa a palavra. O risco previsto no `01-plano.md` (*"a saída antecipada escapa por outro `GATE_WHY` que contenha a palavra"*) é real para os outros gates, que têm cada um a sua frase — confira antes de ancorar em prosa e, se não houver token estável, crie um, como o plano manda.
- 2026-08-26 15:35 · `EXEC` · I3 `done` em `4c9b080`. Baseline antes de tocar em nada: `tests/run-all.sh` → `suite green`, rc 0, 580 `ok`; `check-autonomy.sh` → rc 0, **184**. Depois: `suite green`, rc 0, **581** `ok`; `check-autonomy.sh` → rc 0, **185**, exatamente o 1 a mais do Check.
- 2026-08-26 15:35 · `EXEC` · Red observado antes da implementação e **pelo motivo certo**: as duas metades do diferencial responderam `3|2|blocked|no-progress · 3|2|blocked|no-progress` — ou seja, o handoff `blocked` girava idêntico a uma reprovação ordinária, que é o defeito. A segunda metade já passava no vermelho, de propósito: ela não mede o defeito, mede o conserto não ir largo demais.
- 2026-08-26 15:35 · `EXEC` · Desvio do plano com justificativa: o `01-plano.md` põe a saída nova *"antes do teste de `moved2`"*; foi implementada antes do teste de **`moved`**, na primeira volta. A Métrica 4 (*"sai 3 na primeira sessão"*) só fecha aí — um ramo antes de `moved2` já custou a sessão de retry. O ponto de amostragem de `moved2` **não** foi movido, que é o que o plano de fato proíbe.
- 2026-08-26 15:35 · `EXEC` · ⚠️ **Marcador, não prosa.** O ramo lê o global `GATE_HANDOFF_BLOCKED` (forma do `GATE_EXEC_DIRTY`, `bin/sdd:493`) e nunca um substring de `GATE_WHY` — a palavra "blocked" também aparece na linha de Jidoka do `gate_EXEC` (`:534`), e era esse o risco nomeado no `01-plano.md`. O contrato está escrito acima do global: quem ensinar o token a um **segundo** gate reseta o marcador na **entrada daquele gate**, nunca no call site, porque `current_phase` percorre todos os gates em ordem e um marcador esquecido escalaria a fase errada.
- 2026-08-26 15:35 · `EXEC` · Passada de sabotagem adversarial rodada nos **dois** sentidos, em sandbox, porque uma metade sozinha do diferencial é decoração: ramo neutralizado (`if false`) → a 1ª metade responde `3|2|…|no-progress`; ramo largo demais (`if true`) → a 2ª responde `3|1|…|handoff-blocked` e mais 5 asserções antigas morrem junto. O mutante `RUN_blocked_not_escalated` foi verificado pelo critério do próprio catálogo: controle `SDD_MUTANT=1 run-all.sh` rc **0**, mutante rc **1** com **1** FAIL.
- 2026-08-26 15:35 · `EXEC` · **Para o I4 e para quem for carimbar:** `MUTATION_STAMP_PATHS=(bin tests templates config)` (`bin/sdd:880`) — `agents/` **não** está na chave, então o I4 (que edita `agents/sdd-qa.md` + `sdd install --force`) **não** invalida o carimbo. Logo `4c9b080` é o último commit de código que invalida, e `./bin/sdd health --with-mutation` pode rodar de agora em diante. ⚠️ `tests/health-baseline.txt` **invalida** (está sob `tests/`), então registrar achado no `TODO.md` depois do carimbo o mata — a colisão já está no `TODO.md` do kit.
- 2026-08-26 15:52 · `EXEC` · I4 `done` em `cf43bb3`. Baseline antes de tocar em nada: `tests/run-all.sh` → `suite green`, rc 0; `./bin/sdd preflight` → `preflight ok`. Depois: `suite green`, rc 0, **581** `ok`, 0 FAIL; `preflight ok` com `7 kit agent(s) checked` e working tree limpo. **Com isto não sobra linha `pending` na tabela** — o `20-handoff-exec.md` foi escrito e a fase EXEC fecha.
- 2026-08-26 15:52 · `EXEC` · Red observado antes da edição: `grep -c 'do not block the pipeline' agents/sdd-qa.md` → **1** (esperado `0`); depois → `0`. A outra metade do Check (`cmp -s` fonte × espelho) já respondia `espelho=0` no vermelho, de propósito: ela não mede o defeito, mede o conserto não esquecer o `install --force`. Que ela **não é constante** foi medido, e não suposto: `git show 1fa833f:.claude/agents/sdd-qa.md` reconstrói o espelho no estado "fonte editada, install ainda não rodado", e `cmp -s` contra a fonte de hoje responde `espelho=1`. A primeira redação desta nota afirmava a medição na ordem cronológica sem tê-la feito — foi refeita pelo método acima antes de virar asserção.
- 2026-08-26 15:52 · `EXEC` · Além das duas edições do plano (o `§ 5` e o dever de marcar), entrou uma terceira, pequena e da mesma família: `agent`/`human` foi acrescentado à lista de contrato da seção `Language`. Motivo — este repo declara `OUTPUT_LANG=pt-BR`, o `§ 5.1` manda o agente **escrever** o gênero, e gênero traduzido é gênero que a Âncora 3 não lê; o que ela não lê, conta como ausente, que barra.
- 2026-08-26 15:52 · `EXEC` · ⚠️ **Desvio de escopo deliberado, com justificativa medida — leia antes de chamar de fuga de escopo.** O `§ 5.1` empurrou a regra não-negociável de `agents/sdd-qa.md:142` para `:172`, e **três comentários vivos** citavam a 142 pelo número: `bin/sdd` (2×) e `tests/check-gates.sh` (1×). Hoje a linha 142 cai no meio do `§ 6`. Foram reancorados no **mesmo commit** — é o que o `CLAUDE.md` manda para contrato quebrado em mais de um lugar — e no **heading** (`"Rules that are not negotiable"`) em vez do número, que é o que impede a próxima podridão. Não virou item de `TODO.md` pela régua do D15 (não é fail-open, não tem consumidor fora do kit) e porque a varredura `grep -n '\.md:[0-9]' bin/sdd tests/*.sh agents/*.md` mostrou que **todo anchor restante é PROVENANCE de skill de terceiro**, apontando para fora do repo, onde a forma é deliberada. Custo incremental do desvio: **zero** — o carimbo de mutação já estava inválido desde `525b216`, então tocar `bin/` e `tests/` não comprou volta nenhuma.
- 2026-08-26 15:52 · `EXEC` · ⚠️ **Correção da nota do I3:** ela diz que `4c9b080` é o último commit de código que invalida o carimbo. Deixou de ser verdade pelo item acima — o último é **`cf43bb3`**, e é depois dele que o `sdd health --with-mutation` rodou. O que continua valendo: `agents/` não está em `MUTATION_STAMP_PATHS` (`bin/sdd:880`), então a edição do agente sozinha **não** invalidaria. Carimbo tirado e verde: `kit healthy`, `mutation: score: 150 caught, 0 known gap(s), of 150`, `all 8 gates have a mutation in the catalogue`, `ratchet: 1 known debt(s), none new`. Os dois mutantes desta missão estão registrados na lista do catálogo, e não só definidos — `QA_bug_genre_ignored` (`tests/check-mutation.sh:1814`) e `RUN_blocked_not_escalated` (`:1849`), conferidos por `grep` porque "está no arquivo" e "está na lista" são coisas diferentes.
- 2026-08-26 13:06 · `PLAN` · ⚠️ `sdd health --with-mutation` roda **depois do último commit de código** (a chave do carimbo é o conteúdo de `bin/ tests/ templates/ config/`). Medido nesta máquina em 2026-08-25: 12min32s.
- 2026-08-26 16:25 · `QA` · Ramo **sem interface** (`E2E_CMD=""`, sem `APP_URL`), então não há skill `qa-report`/`qa-execution` e **não há registry para esta missão** — o do próprio kit tem 6 bugs, 0 `open`, como o `01-plano.md` avisou. Logo os dois `F<n>` **não nascem de um `BUG-<id>`**: nascem dos achados 1 e 2 do `30-handoff-qa.md`, que é onde a procedência de cada um está escrita. Nenhum arquivo de `docs/qa/` foi criado, editado ou apagado — a árvore é das skills.
- 2026-08-26 16:25 · `QA` · **F1 nasce do achado 2.** A âncora do gênero (`bin/sdd:639`) não tem fronteira à direita, então ela casa por **prefixo**: medido com o fixture do `check-gates.sh`, `humano`, `humans`, `human-ish` e `humanoid` todos fazem a fase andar para `REVIEW` (o bug deixa de barrar), enquanto `Human` e `HUMAN` barram (o casamento é sensível a caixa). `humano` é a grafia pt-BR e **este repo declara `OUTPUT_LANG=pt-BR`** — ou seja, a tradução que o contrato proíbe é exatamente a que passava. O `agents/sdd-qa.md`, seção `Language`, afirma o contrário com todas as letras: *"a translated genre is a genre the gate cannot read, and it reads as absent, which blocks"*. É **fail-open** na direção permissiva, que é a que a decisão 3 do grill existe para recusar.
- 2026-08-26 16:25 · `QA` · **F2 nasce do achado 1.** O ramo de escalada imediata do I3 está só na **primeira volta** (`bin/sdd:3498`); o retry inline (`:3538`) chama o mesmo gate, que arma o mesmo marcador, mas embaixo dele só se lê `moved2`. Sessão de retry que declara `blocked` e **commita** a declaração lê como *"moved forward, carrying on"* e compra a volta que esta missão existe para apagar. Medido em fixture: **3 sessões** contra as **2** que o conserto entrega, mesmo `rc 3` e mesmo `kind: handoff-blocked`. Alcançável sem contorcionismo — `moved=false` na sessão 1 é justamente o que **convoca** o retry.
- 2026-08-26 16:25 · `QA` · Red observado nos dois sensores **antes** de qualquer conserto, e pelo motivo certo, não só vermelho: o do gênero respondeu `REVIEW|REVIEW` (esperado `REVIEW|QA`) — a metade exata já estava certa, então a falha isola só o prefixo; o da escalada respondeu `3|1|…handoff-blocked · 3|3|…handoff-blocked` (esperado `… · 3|2|…`) — a metade de primeira volta já estava certa, então a falha isola só o retry. Um FAIL em cada arquivo, **zero** colateral: `check-gates.sh` 132 `ok` + 1 FAIL, `check-autonomy.sh` 184 `ok` + 1 FAIL (a 185ª linha `ok` do arquivo íntegro é o **banner** final, impresso só quando `fails -eq 0`, e não uma asserção perdida — a asserção `the repo under test ends with a clean tree` continua verde, que é o que prova que o regime novo devolve o fixture no estado em que o achou).
- 2026-08-26 16:40 · `QA` · **Segunda passada, adversarial, e ela achou o caro — os dois `F<n>` cresceram por causa dela.** A primeira passada mediu o sintoma barato de cada achado; a segunda perguntou "que OUTRO sintoma esta mesma linha produz?". (a) **F2 não custa uma sessão, custa a fase errada.** O marcador é global, `current_phase` roda em **subshell** e não limpa a cópia do pai, e `gate_EXEC` — como todo gate que não é o `gate_QA` — nunca o toca. Então a volta que o retry compra roda `gate_EXEC` com o marcador ainda `1`, e o leitor do `:3498` dispara **para EXEC**: o runner imprime *"the phase's handoff declares 'status: blocked'"* sobre o `20-handoff-exec.md`, que diz `done`, engole o retry que a EXEC tinha direito, e grava `{phase: EXEC, kind: handoff-blocked}` no ledger que o juiz do kaizen lê. É exatamente a falha que o CONTRATO do `bin/sdd:508-511` avisa para um **segundo setter** — alcançada com só o primeiro, porque o marcador sobrevive à VOLTA e não a um gate. ⚠️ E o gatilho é o pipeline seguindo as próprias instruções: o `§ 4` do `agents/sdd-qa.md` manda a QA responder bug sanável com linha `F<n>` `pending`, e linha `pending` é justamente o que manda a volta seguinte para EXEC. **A QA fazendo o trabalho dela é o gatilho.** (b) **F1 não é só grafia, é posição.** `grep -q` é verdadeiro para QUALQUER linha do arquivo, então bug cujo campo diz `agent` — o default que barra — deixa de barrar assim que o corpo **cita** a linha humana, num bloco cercado ou num repro. O `§ 5.1` imprime essa linha exata para o agente copiar, então bug ARQUIVADO SOBRE O CAMPO é a primeira vítima provável. A assimetria é o que faz disso defeito e não mania: a âncora irmã (`Status: open`) também casa em qualquer linha, mas lá casar em qualquer lugar só pode fazer o bug **barrar**; esta é a primeira do gate em que casar em qualquer lugar é **permissivo** — e o `bin/sdd:631-637` chama a âncora de *"strict about it"* sendo estrita só contra a armadilha da legenda.
- 2026-08-26 16:40 · `QA` · Reprodução dos dois sintomas novos, medida e não deduzida — e o **controle diferencial** é o que impede a asserção de passar vazia. (a) Fixture em que o retry declara `blocked` **e** acrescenta uma linha `F1 pending` (que é o que uma sessão de QA de verdade faz): `{phase: EXEC, kind: handoff-blocked}`, 3 sessões, e a EXEC sem o retry dela. Controle com o **mesmo** fixture e a **mesma** linha `F1 pending`, mudando só a palavra do handoff da QA para `done`: `{phase: EXEC, kind: no-progress}`, 4 sessões. Uma palavra no handoff da **QA** troca a escalada da **EXEC** de motivo verdadeiro para motivo falso. O controle também prova que o fixture de fato **chega** na EXEC — sem ele, "escalona QA" passaria num fixture que nunca saiu da QA. (b) Bug com `Status: open` + `Closable by: agent` + a linha humana citada num bloco ```` ```md ````: `phase=REVIEW`, gate passa, `why` diz *"registry clean, suites green"*. Medido também, e **limpo**: o campo é inerte nos outros 4 status (`fixed`/`verified`/`wont-fix`/`invalid` × `agent`/`human`, 8 regimes, todos `REVIEW`) — a Âncora 3 só consulta o gênero em arquivo que o grep de `Status: open` já listou.
- 2026-08-26 16:40 · `QA` · Red observado nas 2 asserções novas, e as duas metades de controle já estavam **verdes** antes do conserto, que é o que prova que cada par isola uma coisa só: a do gênero respondeu `REVIEW|REVIEW` (esperado `REVIEW|QA`) e a da escalada respondeu `EXEC|handoff-blocked · EXEC|no-progress` (esperado `QA|handoff-blocked · EXEC|no-progress`). Os 4 sensores juntos: `check-gates.sh` 132 `ok` + **2** FAIL, `check-autonomy.sh` 184 `ok` + **2** FAIL.
- 2026-08-26 16:25 · `QA` · ⚠️ **Os dois `F<n>` já estão de-riscados — o conserto foi medido numa CÓPIA do repo em `/tmp`, nunca aqui.** `sdd-qa` não conserta produção, mas Check que ninguém sabe se é satisfazível é Check que pode custar uma volta inteira ao executor. Medido com as **4** asserções no lugar: (F1) extrair primeiro a linha do campo — `genre_line="$(grep -m1 -E '^\-[[:space:]]+\*\*Closable by:\*\*' "$bugfile" || true)"` — e só então casar `…[[:space:]]+human([[:space:]]|$)` **contra ela**, por herestring; (F2) o mesmo ramo do `:3498` copiado para depois do `gate_"$phase"` do retry (`:3538`). Resultado: `check-gates.sh` 132 → **134** `ok` rc 0, `check-autonomy.sh` 185 → **187** `ok` rc 0. ⚠️ Três armadilhas na forma, todas medidas: a âncora **tem de aceitar a legenda** `human <!-- agent | human -->` que os arquivos reais preservam (um `$` puro no fim reprova a metade exata do diferencial); a extração do campo resolve **posição e grafia de uma vez**, e consertar só a grafia deixa a metade `the genre is the FIELD` vermelha; e o casamento final vai por **herestring**, nunca `grep -m1 … | grep -q …`, que é a família de `pipefail` que este repo já paga (`grep` que acha e sai antes devolve 141).
- 2026-08-26 16:25 · `QA` · ⚠️ **Para quem executar F1 e F2: os dois ramos novos nascem SEM mutante**, e o precedente é desta própria missão — o I2 e o I3 entregaram cada um o seu (`mut_QA_bug_genre_ignored`, `RUN_blocked_not_escalated`). Sem isso o catálogo segue dizendo `all 8 gates have a mutation` enquanto as duas linhas que os `F<n>` acrescentam não têm quem as meça. ⚠️ E ancore no **código**, nunca em `.*Closable by.*human.*` nem em `.*GATE_HANDOFF_BLOCKED.*`: as duas frases vivem em comentário logo ao lado, e mutante que reescreve só comentário **aplica**, passa da guarda `cmp -s` e não sabota nada — é a lição que o I2 já pagou e que está na nota das 17:05.
- 2026-08-26 17:50 · `EXEC` · F1 `done` em `878b37c`. Baseline antes de tocar em nada: `run-all.sh` rc 1, **2 suítes** vermelhas — `check-gates.sh` 132 `ok` + 2 FAIL, `check-autonomy.sh` 184 `ok` + 2 FAIL —, exatamente o estado que o `30-handoff-qa.md` descreve. Depois: `check-gates.sh` rc 0, **134** `ok`, 0 FAIL, o número que a QA mediu na cópia. As **3** asserções antigas do gênero foram re-andadas e seguem verdes (o re-walk que o Check pede), e o `check-lang.sh` passa com o `humano` novo na prosa do `bin/sdd` — é citação de um valor de dado, não prosa em português.
- 2026-08-26 17:50 · `EXEC` · Red observado antes da implementação e **pelo motivo certo**: as duas responderam `REVIEW|REVIEW` com `REVIEW|QA` esperado. A metade exata (`human` de verdade → `REVIEW`) já estava verde nas duas, então cada falha isola **uma** coisa só — o prefixo numa, a posição na outra — e nenhuma das duas é satisfeita por um conserto largo demais que barre tudo.
- 2026-08-26 17:50 · `EXEC` · ⚠️ **A metade `./tests/run-all.sh` → `suite green` do Check do F1 NÃO é satisfazível pelo F1 sozinho, e não é defeito do conserto.** F1 e F2 **compartilham** essa cláusula, e só o último dos dois pode cumpri-la: os sensores do F2 são vermelhos por construção desde `142c3c3`. Medido depois do F1: `run-all.sh` rc 1, **1** suíte vermelha (era 2), **582** `ok`, e os **únicos** 2 FAIL são as duas linhas nominais do F2 no `check-autonomy.sh` — zero colateral no que o F1 tocou. Aritmética para quem fechar o F2: 582 + 2 asserções + **1 banner** (a linha `ok` final do `check-autonomy.sh` só é impressa com `fails -eq 0`) = **585**, que é exatamente o número que a QA previu. Se der diferente, o F2 mexeu em algo além do que nomeia.
- 2026-08-26 17:50 · `EXEC` · ⚠️ **Podridão de âncora, verificada e não suposta.** O conserto reescreve a **exata linha** em que o `mut_QA_bug_genre_ignored` (`tests/check-mutation.sh:239`) ancora. Conferido em sandbox antes de commitar: continua aplicando em **1** linha e mata **4** asserções — a âncora sobrevive porque prende o prefixo `^    if grep -qE` e o sufixo `then continue; fi$`, que nenhum comentário tem. Quem mexer nessa linha de novo repita a conferência; o Jidoka do `01-plano.md` manda **reancorar, nunca remover**.
- 2026-08-26 17:50 · `EXEC` · Os dois ramos novos nasceram **com** mutante, como a QA e o precedente do I2/I3 exigem, e são **dois** e não um porque as duas metades falham aberto independentemente — um mutante só deixaria uma delas apodrecer com o catálogo verde. `QA_bug_genre_prefix` (delimitador `@`, porque o texto casado **é** uma alternação e um script delimitado por `|` seria rejeitado pelo sed) e `QA_bug_genre_anywhere`. Verificados pelo critério do próprio catálogo, em sandbox com controle rc 0: 1 linha cada, `bash -n` verde, e cada um mata **exatamente** a asserção que nomeia a sua propriedade — 1 FAIL, não 4. Os dois estão na lista `CATALOG` (`:1843-1844`) e não só definidos.
- 2026-08-26 17:50 · `EXEC` · Limite **declarado** no comentário do `bin/sdd`, pela régua do D15, em vez de virar probe inventado: `-m1` significa "a primeira linha com forma de campo **é** o campo", que é a ordem do próprio template (campos no cabeçalho, corpo depois). Arquivo que citasse a linha humana **acima** do próprio campo ainda falharia aberto — malformado pelo template, e nenhum probe o prende.
- 2026-08-26 17:50 · `EXEC` · **Para quem executar o F2:** o carimbo de mutação segue **morto** e não foi tirado aqui de propósito — `878b37c` toca `bin/` e `tests/`, e o F2 vai tocar os dois de novo. Rode `./bin/sdd health --with-mutation` **depois do commit do F2** (~12min32s), e conte **152** mutantes, não 150: os dois desta linha entraram. ⚠️ O catálogo roda com controle `SDD_MUTANT=1 run-all.sh` exigido rc **0**, então ele é insatisfazível enquanto o F2 estiver `pending` — não tente carimbar antes.
- 2026-08-26 16:25 · `QA` · `./bin/sdd preflight` agora acusa **2 fails** (`TEST_CMD FAILED` e `working tree dirty`), e os dois são **desta sessão**: os sensores vermelhos são o artefato que o `§ 3` do `agents/sdd-qa.md` manda commitar, e a árvore suja é este commit. Antes da sessão era `preflight ok` (medido pela EXEC). Os dois limpam com F1 e F2 verdes — não são achado, são o estado esperado do laço QA⇄EXEC.
- 2026-08-26 18:35 · `EXEC` · F2 `done` em `26b74a0`. Baseline antes de tocar em nada: `run-all.sh` rc 1, **1** suíte vermelha, `check-autonomy.sh` 184 `ok` + 2 FAIL, `check-gates.sh` rc 0 e 134 `ok` — exatamente o estado que a nota do F1 previu. Depois: `check-autonomy.sh` rc 0, **187** `ok`, 0 FAIL, o número que a QA mediu na cópia; `run-all.sh` → `suite green`, rc 0, **585** asserções — que é 582 + 2 + 1 banner, a aritmética que a nota do F1 deixou escrita. Bateu na casa, então o F2 não mexeu em nada além do que nomeia.
- 2026-08-26 18:35 · `EXEC` · Red observado antes da implementação e **pelo motivo certo**: `3|1|blocked|handoff-blocked · 3|3|blocked|handoff-blocked` (esperado `· 3|2|…`) e `EXEC|handoff-blocked · EXEC|no-progress` (esperado `QA|handoff-blocked · …`). Em cada par a metade de controle já estava **verde**, então cada falha isola uma coisa só — o retry numa, a fase nomeada na outra — e nenhuma é satisfeita por um conserto largo que escale todo retry.
- 2026-08-26 18:35 · `EXEC` · **Re-walk das duas metades de cada diferencial, com VALOR e não com "passou"**, que é o que o Check pede. Feito numa cópia em `/tmp` com as três `assert_eq` precedidas de um `echo` das variáveis compostas, e com **piso contra vacuidade** (`grep -c '^echo "WALK'` = 3 antes de rodar, senão aborta — trace que não aplicou faria o walk concluir vazio). Saída: primeira volta `3|1|blocked|handoff-blocked` × `3|2|blocked|no-progress`; retry `3|1|…handoff-blocked` × `3|2|…handoff-blocked`; fase `QA|handoff-blocked` × `EXEC|no-progress`. As três `ok` nominais presentes, rc 0.
- 2026-08-26 18:35 · `EXEC` · Desvio da forma que a QA mediu, com justificativa. A QA prescreveu *"o mesmo ramo do `:3498` copiado para depois do `gate_"$phase"` do retry"*; foi implementado como **uma definição e duas portas** (`handoff_blocked_escalation()`), depois de o verde ser obtido primeiro com a cópia literal (187 `ok`) e só então refatorado (187 `ok` de novo — é essa a evidência de que o refactor preservou comportamento). Motivo: a cópia são 12 linhas com 4 mensagens de prosa e a string do `kind` do ledger, e duas cópias à mão são a deriva que o `CLAUDE.md` já nomeia. Onde as portas **ficam** continua no laço — a forma do `kit_guard_check`, paga como ele, com um mutante por porta.
- 2026-08-26 18:35 · `EXEC` · ⚠️ **Podridão de âncora, verificada e não suposta — e desta vez ela ACONTECEU.** A extração apagou a linha em que o `mut_RUN_blocked_not_escalated` ancorava: `grep -cE '^    if \[ "\$GATE_HANDOFF_BLOCKED" = "1" \]; then$' bin/sdd` → **0**. Reancorado, nunca removido, como o Jidoka 3 do `01-plano.md` manda. ⚠️ **A âncora nova é RANGED por necessidade:** desde este commit as duas portas são o **mesmo texto**, então um `s|…|…|` sem range reescreveria as duas e nenhum dos dois mutantes isolaria porta nenhuma. Os delimitadores são as linhas de código que cercam cada porta (`gate_rc`/`moved` e `gate_rc2`/`moved2`), as quatro conferidas com `grep -c` → 1 cada.
- 2026-08-26 18:35 · `EXEC` · Os mutantes são **dois** (`RUN_blocked_not_escalated` reancorado + `RUN_blocked_retry_not_escalated` novo) porque as portas falham aberto de forma independente — mesmo argumento que separou `QA_bug_genre_prefix` de `QA_bug_genre_anywhere` no F1. Verificados em sandbox **extraindo a função verbatim do catálogo com `sed`**, nunca redigitada: controle rc 0 / 187 `ok`; cada um aplica em **1** linha, e em linhas **distintas** (3556 × 3598 — conferido, porque "1 linha cada" não prova que não é a mesma linha duas vezes); `bash -n` verde; e cada um mata a asserção **exclusiva** da sua porta (o 1º mata a de primeira volta, que o 2º não toca; o 2º mata a de fase nomeada, que o 1º não toca). Nenhum subsume o outro.
- 2026-08-26 18:35 · `EXEC` · Limite **declarado** no cabeçalho do catálogo, pela régua do D15, em vez de virar um terceiro mutante: o **corpo** (`handoff_blocked_escalation`) não ganha mutante próprio porque só é alcançado pelas duas portas — neutralizá-lo é indistinguível de neutralizar as duas de uma vez, evidência estritamente mais fraca que o par.
- 2026-08-26 18:35 · `EXEC` · **Nit visto e PRECIFICADO, não despercebido** (para a REVIEW não o achar como novo): o cabeçalho da família no `tests/check-mutation.sh:766-770` e o comentário da Porta 1 logo abaixo repetem a mesma frase de medição (`7 das 12 sessões de QA / US$ 73,32`). É prosa redundante, não fail-open. Não foi consertado porque `tests/` é chave do carimbo de mutação e a edição chegaria **com o `sdd health --with-mutation` em voo** — 12min32s por uma linha de comentário. Quem mexer nesse arquivo por qualquer outro motivo, leve junto; sozinho não paga a volta.
- 2026-08-26 18:35 · `EXEC` · Entrou junto, e é contrato e não escopo extra: o comentário do `GATE_HANDOFF_BLOCKED` (`bin/sdd:508-511`) afirmava que *"o único leitor é o ramo no laço do cmd_run"* — falso desde este commit — e a QA já tinha registrado em "Riscos" que ele descrevia garantia mais forte que a do código. Passa a dizer que o marcador não sobreviver a volta nenhuma é consequência do **fluxo** (toda volta que o acha armado devolve 3) e não da construção, com o aviso para quem acrescentar uma terceira porta ou um segundo setter. `CLAUDE.md`: contrato mudado se atualiza no **mesmo commit**.

> **Toda vez que um humano precisou entrar na linha** — um `sdd retry`, um conserto à mão, um
> `BLOCKED` assumido — sai uma linha com o marcador `intervention:`. É o que o
> `sdd autonomy --by-mission` conta, e é a metade que o custo sozinho não mostra: US$ baixo não
> distingue "rodou barato" de "rodou barato porque um humano fez metade".
>
> ⚠️ O marcador é **inglês e minúsculo**, como `pending`/`done`/`blocked`: é contrato, não prosa.
> O texto depois dos dois-pontos vai no idioma do `OUTPUT_LANG`, como o resto deste arquivo.
> Só conta quando abre a linha — `intervention` no meio de uma frase é prosa e não é contado.
>
> O exemplo abaixo mora **dentro** desta citação de propósito: o `>` quebra o casamento com
> `^[[:space:]]*-`, e sem ele o exemplo era contado verbatim — todo checkpoint recém-instanciado
> nascia devendo uma intervenção fantasma ao instrumento que mede autonomia. Copie a forma para
> fora da citação ao registrar uma intervenção de verdade.
>
> - intervention: <o que o humano teve de fazer> — <fase> — <custo, se houver>

## Incrementos de fix (QA)

> Escritos pelo `sdd-qa` quando um bug sanável é reprovado. Entram na mesma tabela acima com ID
> `F<n>`, e o Check obrigatoriamente inclui **regression test passa** + **re-walk da jornada
> impactada verde**. Bug que exige julgamento humano NÃO vira fix — vai para
> "Decisions for a Human" no handoff de QA.
