---
missao: 20260901-o-revisor-so-acha
atualizado: 2026-09-02 00:30
---

# Checkpoint — O revisor só acha

> **Este arquivo é lido por máquina.** O runner faz parse da tabela abaixo para decidir a
> próxima fase. Não mude as colunas, não mude os tokens de status, não quebre linhas dentro de
> uma célula. Detalhe narrativo vai no `01-plano.md`, não aqui.
>
> ⚠️ **Nada de `|` cru na célula do Check.** A tabela é lida com `awk -F'|'`: um pipe cru parte a
> célula em duas, o Status lido passa a ser um pedaço do comando e o Commit passa a ser `pending`.
> O `gate_EXEC` reprova por "invalid status" e o `sdd status` imprime algo de aparência saudável —
> custou uma missão inteira até alguém olhar. O escape do GFM, `\|`, o runner **hoje entende**:
> `checkpoint_rows` remonta a célula por paridade de `\`, como o `gate_REVIEW` já fazia. Isso é
> rede de segurança, não licença — Check que precisaria de pipe continua virando herestring:
> `` o=$(cmd 2>&1); grep -c 'x' <<< "$o" ``, e o `tests/check-checkpoint.sh` recusa as duas formas
> nos checkpoints deste repo.
>
> ⚠️ **Check que lê a saída de um sensor ancora em `^  ok    ` — quatro espaços, com o `^`.**
> Todo sensor da suíte imprime `  ok    <asserção>` na **stdout** e `  FAIL  <asserção>` na
> **stderr**, com o *mesmo* `<asserção>`. Um Check que faz `2>&1` e grepa o texto solto devolve o
> mesmo número com a asserção verde e com ela vermelha: ele responde "a asserção existe", nunca
> "a asserção passou". Custou uma missão inteira, achado só na fase QA. A forma certa:
> `` o=$(bash tests/check-x.sh 2>&1); grep -c '^  ok    <asserção>' <<< "$o" `` → `1`.
> O sensor é `tests/check-checkpoint.sh`.
>
> Atualizar o checkpoint é o **último ato** de cada incremento — depois do commit, nunca antes.
> Status válidos: `pending` · `doing` · `done` · `blocked`.

| ID | Incremento | Check (comando → esperado) | Status | Commit |
|---|---|---|---|---|
| I1 | o ledger carrega `turns`, e `sdd autonomy --by-mission` imprime o laço de revisão | `bash -n bin/sdd; o=$(bash tests/check-autonomy.sh 2>&1); grep -c '^  ok    a session row carries the turns the session spent' <<< "$o"; grep -c '^  ok    the review loop counts REVIEW and the EXEC sessions after it, never the EXEC before' <<< "$o"` → `1` e `1` | done | 88432ee |
| I2 | o contrato: o revisor só acha, achado vira incremento R — agente, executor, templates, prompt e `docs/pipeline.md` no mesmo commit | `o=$(bash tests/check-gates.sh 2>&1); grep -c '^  ok    a review graded B with a pending R1 hands the ball to EXEC' <<< "$o"; o2=$(bash tests/check-dry-run.sh 2>&1); grep -c '^  ok    the REVIEW boot prompt sends findings to R increments and never fixes in-session' <<< "$o2"; diff -q agents/sdd-reviewer.md .claude/agents/sdd-reviewer.md; echo rc=$?` → `1`, `1` e `rc=0` | done | 03187e8 |
| I3 | a guarda de aviso `REVIEW-EDITED-CODE`: sessão REVIEW que editou código fora do diretório da missão é registrada no `pipeline.log` | `o=$(bash tests/check-autonomy.sh 2>&1); grep -c '^  ok    a REVIEW session that edited code outside the mission directory is logged REVIEW-EDITED-CODE' <<< "$o"; grep -c '^  ok    a REVIEW session that only wrote the round and the checkpoint is not flagged' <<< "$o"` → `1` e `1` | done | c8c8ec7 |
| I4 | docs: schema, failure-modes, `CONTEXT.md`, `KAIZEN_LOG.md` (antes), e `docs/graphify.md` entra na superfície do `check-lang` | `o=$(grep -l 'o-revisor-so-acha' KAIZEN_LOG.md CONTEXT.md docs/pipeline.md config/schema.md docs/failure-modes.md); wc -l <<< "$o"; s=$(sed -n '/^surface()/,/^}/p' tests/check-lang.sh); grep -c 'docs/graphify.md' <<< "$s"; bash tests/check-lang.sh >/dev/null 2>/dev/null; echo rc=$?` → `5`, `1` e `rc=0` | done | a84adeb |
| F1 | `templates/review.md` para de mandar a rodada registrar o que ela corrigiu: as linhas 19 e 64-65 apontam para uma seção que começa com "Esta rodada não conserta" e não tem coluna de hash | `grep -c 'virou correção' templates/review.md; grep -c 'o que foi corrigido' templates/review.md; bash tests/check-templates.sh >/dev/null 2>/dev/null; echo rc=$?` → `0`, `0` e `rc=0` | done | 384e36c |
| F2 | a asserção do heading do checkpoint no `check-templates.sh` casa o heading ANTIGO — apertar para o nome novo, como o irmão dela em `:217-220` já faz | `cp templates/checkpoint.md /tmp/f2.bak; sed -i 's/ (QA e REVIEW)/ (QA)/' templates/checkpoint.md; bash tests/check-templates.sh >/dev/null 2>&1; echo old=$?; cp /tmp/f2.bak templates/checkpoint.md; bash tests/check-templates.sh >/dev/null 2>&1; echo new=$?` → `old=1` e `new=0` | done | 48097cd |
| F3 | `README.md:152` ainda promete que o `sdd-reviewer` entrega `+ fixes` — o sexto lugar do contrato que o I2 mudou em cinco | `grep -c 'sdd-reviewer.*fixes' README.md; bash tests/check-lang.sh >/dev/null 2>/dev/null; echo rc=$?; bash tests/run-all.sh >/dev/null 2>/dev/null; echo suite=$?` → `0`, `rc=0` e `suite=0` | done | c05b43b |
| F4 | a allowlist da guarda normaliza `HANDOFF_DIR` (barra final vira falso positivo em TODA rodada saudável) e declara o limite do braço `tests/health-baseline.txt` | `o=$(bash tests/check-autonomy.sh 2>&1); grep -c '^  ok    a trailing slash in HANDOFF_DIR does not turn a healthy round into a warning' <<< "$o"; bash tests/run-all.sh >/dev/null 2>&1; echo suite=$?` → `1` e `suite=0` | pending | — |

## Notas de execução

> Uma linha por evento relevante: bloqueio, decisão tomada, desvio do plano com justificativa.
> É o que a próxima sessão lê para não repetir um erro que já custou caro.

- 2026-09-01 18:30 · `PLAN` · plano nascido do grill com o humano presente (5 perguntas, 5 decisões); commit do grill já em disco (graphify + `CONTEXT.md` D22/D23). Ordem obrigatória: I1 → I2 → I3 → I4.
- 2026-09-01 · `I1` · `88432ee`. Desvio justificado do plano: o stub do bloco `== session rows ==` não podia só ganhar `"num_turns": 7` — ele saía 1 **sem escrever stream nenhum**, e `run_phase` lê o turno do mesmo `result` destilado de onde lê o custo. O stub passou a responder com a amostra capturada **menos o dinheiro e mais o turno** (derivada por `jq` do `STREAM_SAMPLE`, nunca colada — regra de proveniência), o que mantém `unknown cost is null, not a string` medindo o mundo que o nome dela diz. `7` e não o `1` da captura de propósito: `attempt` também vale 1 na primeira linha, e um escritor que carimbasse a constante passaria por coincidência.
- 2026-09-01 · `I1` · a asserção `an escalation row carries no turns` já nascia **verde** (o campo não existia em lugar nenhum) — é guarda, não Red, e o plano a pediu assim. O Red observado foram as outras duas, pelo motivo certo: `1 7 0 null` (piso do mundo armado, campo ausente) e `1 none 1 none` (a linha `rl1` imprime, sem a célula).
- 2026-09-01 · `I1` · confirmação independente no ledger real: `20260831-a-rodada-que-andou` lê `review loop US$ 66.34 (50%)`, exatamente o número que o `00-missao.md` cita para o PR #33 — o instrumento reproduz à mão o que o gemba mediu. `sdd autonomy --all-repos --by-mission` imprime 16 células.
- 2026-09-01 · `I2` · `03187e8`. Red observado nas três asserções novas, cada uma pelo motivo certo: a do dry-run reprovou por "no mention of an R<n> increment" (o prompt ainda dizia `review and fix, INSIDE`); a do template, por `missing section 'Incrementos de conserto'`; a do `check-gates.sh` foi medida numa cópia **sem** a linha `R1` do fixture (`grep -v` da linha do `printf`) — respondeu `REVIEW` em vez de `EXEC`, e no mesmo probe a guarda `fixture: the R1 row really closed` morreu alto, provando que ela não conclui sobre um fixture que não foi montado.
- 2026-09-01 · `I2` · desvio justificado do plano: o item (5) pedia também *"a linha `REVIEW` da tabela de fases (`:128` vizinhança) diz findings → R<n>"*. **Essa tabela não existe** — o que está em `docs/pipeline.md:124-128` é a tabela dos três sub-passos da QA, e não há tabela de fases com agente no arquivo. O contrato foi escrito por inteiro no `§ REVIEW` (laço, o que a sessão commita, r1 em B como rodada saudável, gate declarado inalterado) e na frase do laço em `:204`.
- 2026-09-01 · `I2` · o mutante `mut_RUN_review_fixes_inline` nasceu ancorado na **indentação** do arm (`^    REVIEW)   printf`) e foi reescrito para a faixa da função (`/^phase_task()/,/^}/`): `phase_agent` e `phase_model` têm um `REVIEW)` cada, um deles a uma tecla de distância, e um padrão de espaços que derivasse sabotaria o arm errado parecendo aplicado. Provado numa cópia da árvore inteira — `cmp` acusa diferença, `bash -n` compila, e `check-dry-run.sh` fica vermelho em **uma** asserção, a nomeada.
- 2026-09-01 · `I2` · o `tests/check-lang.sh` reprovou `agents/sdd-reviewer.md` numa linha só: a palavra `Pendências` citada como nome de seção do template. A superfície do agente é inglês, então a citação passou a ser a metade inglesa do heading ("Decisions for a Human"). As outras citações de heading PT-BR (`## Incrementos de conserto`, `O que virou incremento`) o dicionário não pega e ficaram — são contrato de template.
- 2026-09-01 · `I2` · piso do `check-templates.sh` recontado 23 → 24 no mesmo diff que acrescenta a regra (piso que fica para trás continua passando descrevendo superfície menor). Catálogo de mutação: 220 → 221, e as duas contagens (definições e `CATALOG=(`) batem.
- 2026-09-01 · `I3` · `c8c8ec7`. Red observado nas duas asserções de aviso pelo motivo certo — `sessions:1 lines:0 warns:0 phase: n: files:` e `retried:1 lines:0 files:`: o piso de cada regime (a sessão rodou; o retry realmente disparou) já estava verde e só a saída da guarda faltava. O controle negativo nasceu **verde**, como o plano pediu: é guarda contra ruído, não Red.
- 2026-09-01 · `I3` · **desvio do plano com número atrás: entrou uma quarta asserção.** O plano pediu três, e a passada de sabotagem mostrou que com elas apagar a chamada do `cmd_retry` deixava a suíte inteira verde (`door1=1 control=1 retry=1 fails=0`) — porta cuja remoção nenhuma asserção percebe, o que o `CLAUDE.md § 5` proíbe em tantas palavras. A asserção `sdd retry is another door that opens a REVIEW session, and it is guarded too` foi escrita no instante em que isso foi medido. Sabotando uma porta por vez numa cópia da árvore, cada uma das três agora derruba só a sua; derrubar o braço `"$HANDOFF_DIR/$MISSION"/*` da allowlist derruba as quatro, que é o que prova que a allowlist é medida.
- 2026-09-01 · `I3` · o `shellcheck` do `run-all.sh` reprovou SC2034 (`RS3_ERR appears unused`). Consertado **usando** a captura (o regime do retry inline passou a exigir `warns:1`) e não apagando-a: o aviso na stderr é metade do que a guarda promete, e um regime que só lê o `pipeline.log` não a mede.
- 2026-09-01 · `I3` · o termo `warns:` lê prosa que **só** existe no `warn` (`the reviewer finds and the executor fixes`), nunca o texto compartilhado com a linha do journal. Um `grep 'outside the mission directory'` responderia o mesmo número lendo o arquivo errado — a mesma classe do `^  ok    ` que este checkpoint já declara no cabeçalho.
- 2026-09-01 · `I3` · a guarda **não** ganhou early-return de `DRY_RUN`. A projeção alcança a função (a chamada fica acima do ramo `DRY_RUN` do `cmd_run`, ao lado da do irmão) e é silenciosa por construção: `run_phase` volta antes de abrir sessão, então os dois heads são o mesmo commit. Diferente do `kit_guard_arm`, nada aqui é alcançado de nenhum outro lugar durante uma projeção, logo não há aviso a herdar. O cabeçalho **declara** qual mundo não foi construído, em vez de afirmar que ele não existe — a cicatriz de `d4deb35`/`7cbc8e2`.
- 2026-09-01 · `I3` · catálogo de mutação 221 → 222 (`mut_RUN_review_scope_blind`), definições e `CATALOG=(` batendo em 222; suíte rápida 845 → 855 asserções. O carimbo segue morto até a fase DOCS.
- 2026-09-01 · `I4` · `a84adeb`. Red observado no Check antes de qualquer edição — `2` (e não `5`) arquivos com o slug e `0` (e não `1`) `docs/graphify.md` na `surface()`. O terceiro termo (`rc=0`) já nascia verde: é guarda contra "entrar na superfície e reprovar", não Red.
- 2026-09-01 · `I4` · **desvio do plano com número atrás: o piso do `check-lang.sh` subiu 37 → 41, não 37 → 38.** O plano pediu "+1" supondo que o piso descrevia a superfície; ele descrevia **37 caminhos contra 40 reais** — as ADRs 0004, 0005 e 0006 entraram pelo glob `docs/adr/*.md` sem tocar o número. É a mesma falha que o comentário do sensor já nomeia uma vez (r1 de `20260817-eixo-do-juiz`), viva de novo: piso que fica para trás continua **passando** enquanto mede superfície menor que a que lê. Recontado contra a superfície real, com o histórico no comentário.
- 2026-09-01 · `I4` · a entrada na superfície foi provada nos **dois** sentidos, e não só pela contagem: tirar `docs/graphify.md` do disco derruba o sensor com `surface shrank to 40 … expected at least 41` (rc 93), e acrescentar uma linha em português ao arquivo derruba com `Portuguese outside the allowlist: docs/graphify.md` (rc 1). Contagem sozinha provaria que o caminho está na lista, nunca que ele é **lido**.
- 2026-09-01 · `I4` · **uma correção de número, registrada antes de o resultado ser conhecido.** O `00-missao.md` cita a janela 2 como `25 · 29 · 60` (máximo 60%), calculado à mão sobre um denominador de US$ 79,00; o instrumento do I1 lê **57%** para a mesma missão, porque o denominador dele são **todas** as sessões da missão (US$ 84,42). A M1 é lida pelo instrumento, então a linha de base honesta é mediana 29% · máximo **57**%. O alvo (mediana ≤ 25%, máximo ≤ 50%) **não** se move por causa disso — mexer no alvo depois de saber o resultado é o modo de falha que o `KAIZEN_LOG` de 2026-08-31 já nomeia.
- 2026-09-01 · `I4` · `CONTEXT.md` conferido contra o que I2/I3 pousaram, função por função (`phase_task REVIEW`, `LAST_PHASE_TURNS`, o sufixo `review loop`, a allowlist do `review_scope_check`): D22 e o verbete **batem**, nada a corrigir. Acrescentado o *como pousou* (um hash por fatia, as três portas da guarda) e a única divergência do plano, que é do I2: a "tabela de fases" do `docs/pipeline.md:128` não existe.
- 2026-09-01 · `I4` · a coluna **Depois** do `KAIZEN_LOG.md` fica em aberto de propósito, com o motivo escrito na própria entrada: quem a preenche é a fase DOCS, com os números da REVIEW **desta** missão. Preenchê-la agora seria previsão com roupa de medição — a régua que este arquivo recusa.
- 2026-09-01 · `I4` · **nenhum achado foi escrito no `TODO.md`**, de propósito: `tests/health-baseline.txt` está na chave do carimbo de mutação. Os três achados (o env-cleanup do `run_phase`, o `surface()` enumerado, e a **classe** "piso que fica para trás" descoberta aqui) estão em `20-handoff-exec.md § Achados fora de escopo`, para a DOCS transportar com a catraca no mesmo diff.
- 2026-09-01 · `I4` · último incremento: `20-handoff-exec.md` escrito do `templates/handoff.md` (não existia antes — esta é a primeira vez que o handoff de EXEC desta missão é escrito, e não uma atualização de rodada). Suíte 855 asserções, `suite green`, rc 0.
- 2026-09-01 · `QA` · nenhum `BUG-<id>` deu origem a estes `F<n>`: o registry `docs/qa/bugs/` **não foi tocado** (é das skills, e um humano o criou) e não tem uma linha `Status: open`. A proveniência de cada fix é uma jornada de terminal caminhada na fase QA, registrada em `30-handoff-qa.md § O que foi feito`. Os quatro foram **reproduzidos antes de virar linha**, cada um com o vermelho medido.
- 2026-09-01 · `QA` · `F1` ← jornada J5 (contrato do artefato que a REVIEW escreve). `templates/review.md:19` manda o TL;DR dizer "o que foi corrigido" e `:64-65` manda o achado que virou correção reaparecer "na seção seguinte, com hash" — mas a seção seguinte é `## Incrementos de conserto (R<n>)` (`:71`), cuja primeira frase é **"Esta rodada não conserta."** e que não tem coluna de hash. O I2 reescreveu `:71-92` e deixou os dois parágrafos que apontam para lá. É o mais urgente dos quatro: a REVIEW desta missão é a **primeira** a ler este template, e ele a empurra de volta para consertar in-session — exatamente o que a missão existe para apagar. Vermelho medido: `virou correção` → 1, `o que foi corrigido` → 1.
- 2026-09-01 · `QA` · `F2` ← **fail-open medido nos dois sentidos**, a classe que o `CLAUDE.md § D15` manda registrar sempre. `tests/check-templates.sh:129` casa `'^## Incrementos de fix'`, que serve ao heading **antigo** `## Incrementos de fix (QA)` tão bem quanto ao novo, enquanto o rótulo da asserção jura pinar `'Incrementos de fix (QA e REVIEW)'`. Reproduzido: revertendo o heading do template para o antigo, `tests/check-templates.sh` responde **rc=0** — o sensor afirma medir o que não mede. O irmão dele em `:217-220` faz certo e diz por quê no comentário ("asserted by its NEW name"), então a forma correta já está no arquivo.
- 2026-09-01 · `QA` · `F3` ← jornada J6. O I2 mudou o contrato em cinco lugares no mesmo commit e o `README.md:152` é o **sexto**: `` | `sdd-reviewer` | code review until Grade A | Opus | `40-review-r<N>.md` + fixes | ``. O revisor não entrega mais `fixes`. `README.md` está na `surface()` do `check-lang.sh`, isto é, é arquivo que o kit **afirma** manter em dia — e `CONTEXT.md:34` enumera os cinco lugares sem ele. É o modo de falha que o `CLAUDE.md` chama de mais caro do kit: contrato quebrado em mais lugares do que o commit tocou.
- 2026-09-01 · `QA` · `F4` ← jornada J5 + leitura da allowlist. `bin/sdd:2250` casa `"$HANDOFF_DIR/$MISSION"/*` contra a saída de `git diff --name-only`, que é uma **string**, não um caminho que o kernel colapse. `HANDOFF_DIR` vem verbatim do `.sdd/config.sh` do repo-alvo e **nunca** é normalizado (`grep -c 'HANDOFF_DIR%/' bin/sdd` → `0`). Reproduzido: com `HANDOFF_DIR="docs/handoffs/"` o próprio `40-review-r1.md` da rodada é marcado `FLAG`, e a guarda passa a gritar `REVIEW-EDITED-CODE` em **toda rodada saudável** — o resultado que o comentário de `:2246-2249` diz estar evitando ("train the guard's only reader to scroll past it") e que quebra a instrução de `docs/failure-modes.md:433`. Vai junto o braço `tests/health-baseline.txt` (`:2252`), que o comentário escopa "in the kit's own repo" sem condição nenhuma no código: em repo-alvo que tenha esse caminho a guarda falha aberta em silêncio, e é a única frouxidão da função **não** declarada em `:2217-2230`.
- 2026-09-01 · `F1` · `384e36c`. Red observado antes de qualquer edição no template, e pelo motivo certo: as duas regras novas reprovaram nomeando a linha exata (`still carries the TL;DR field 'o que foi corrigido'` e `still carries the pointer sending a finding that 'virou correção'`), e o `ok    rule:` do bloco ficou **suprimido** — o ramo que existe para não deixar as duas metades do veredito dividirem o mesmo stream.
- 2026-09-01 · `F1` · **desvio justificado do plano da QA: o conserto não é só de prosa.** A linha do checkpoint pede `grep -c` = 0, e um `grep` no checkpoint morre com a missão; o defeito era o sensor não saber olhar. `tests/check-templates.sh` ganhou `refute()`, o espelho de `check()` que passa quando a regex **não** casa — nenhuma asserção positiva enxerga prosa deixada para trás (todas as seções estavam lá, o gate parseava, e o sensor imprimia `template contract intact` sobre um arquivo que mandava fazer o contrário do contrato).
- 2026-09-01 · `F1` · `refute()` lê o rc do `grep` como **três** valores e só o `1` (ausente) passa. A forma curta `|| rc=1` dobraria o rc 2 no ramo de sucesso e faria arquivo ilegível ou inexistente refutar tudo de graça — a mesma classe de fail-open que o `check()` deste arquivo já pagou com probe. O mundo **foi construído**: sabotar `|| rc=$?` para `|| rc=1` deixa o sensor em rc 92, `an unreadable template refutes everything for free`. Não é "não consegui imaginar o mundo", é o mundo medido.
- 2026-09-01 · `F1` · passada de sabotagem, cada uma derrubando **só** a sua, numa cópia da árvore: `refute()` virado no-op → rc 92 (`the forbidden-prose rules are decoration`); `|| rc=$?` dobrado → rc 92 (probe do arquivo ilegível); uma das duas regras apagada → o piso acusa `only 25 assertion(s) ran, expected at least 26`; o template revertido → a regra acusa a linha. Controle: cópia intocada e cópia restaurada, rc 0 as duas. Um probe de sabotagem que não aplicou **morreu alto** (`SABOTAGE-DID-NOTHING`) em vez de concluir — foi o que aconteceu na primeira tentativa do segundo regime, e a conclusão falsa não chegou a ser escrita.
- 2026-09-01 · `F1` · piso `REVIEW_FLOOR` recontado 24 → 26 **no mesmo diff** que acrescenta as regras. Catálogo de mutação **não** muda (222): ele sabota `bin/sdd`, e este sensor lê `templates/` — é justamente por isso que ele carrega auto-teste, e as três probes novas de `refute()` entram lá. Suíte rápida 855 → 857 asserções.
- 2026-09-01 · `F1` · nada foi escrito no `TODO.md`: nenhum achado fora de escopo nesta sessão, e a rota continua sendo a fase DOCS (`tests/health-baseline.txt` está na chave do carimbo). O carimbo de mutação segue morto — `tests/` e `templates/` mudaram de novo, e ainda faltam `F2`–`F4`.
- 2026-09-01 · `F2` · `48097cd`. Red observado antes de qualquer edição, e é o Red **do meio da célula**: o Check tem dois termos e só o primeiro estava vermelho — `old=0` (o sensor verde sobre o template com o heading ANTIGO) contra `new=0` (o verde legítimo). Depois do aperto, `old=1` nomeando a linha (`FAIL checkpoint.md: missing section 'Incrementos de fix (QA e REVIEW)'`) e `new=0`. Um Check de um termo só teria passado nos dois mundos.
- 2026-09-01 · `F2` · **nada foi acrescentado ao sensor: a regex apertou.** Nenhuma asserção nova (857 antes e depois), então nenhum piso se move — `REVIEW_FLOOR` cobre o bloco do `templates/review.md` e este é o do `checkpoint.md`, que não tem piso próprio. Catálogo de mutação inalterado (222): ele sabota `bin/sdd` e este sensor lê `templates/`, que é exatamente por que ele carrega auto-teste. O carimbo segue morto (`tests/` mudou de novo).
- 2026-09-01 · `F2` · a vizinha `review_check '^## Incrementos de conserto'` tem o mesmo formato e **não** entrou no diff, com medição: heading do template trocado para `(F<n>)` ⇒ sensor `rc=0`. É caso mais fraco (nenhum heading foi substituído, logo a frouxidão não certifica contrato antigo) e está fora da linha que o `F2` nomeia — foi para `20-handoff-exec.md § Achados fora de escopo`, rota da fase DOCS, porque `tests/health-baseline.txt` está na chave do carimbo e escrever no `TODO.md` aqui o mataria. Varredura da família inteira do arquivo: essa é a única.
- 2026-09-01 · `F3` · `c05b43b`. Red medido antes de qualquer edição: `grep -c 'sdd-reviewer.*fixes' README.md` → **1**, devendo ser `0`. Os outros dois termos já nasciam verdes (`rc=0`, `suite=0`) — são guarda contra "consertar a linha e quebrar o idioma ou a suíte", não Red, e é por isso que o Check tem três termos e não um.
- 2026-09-01 · `F3` · **o Check pina a PALAVRA, não a promessa**, e isso mordeu no meio da sessão. A primeira redação da célula — `code review until Grade A (finds; never fixes)` — diz o contrato **certo** e mesmo assim deixou o Check em `1`, porque `sdd-reviewer.*fixes` casa "never fixes" tão bem quanto "+ fixes". A célula passou a dizer `read-only over the code`, que é a frase do frontmatter de `agents/sdd-reviewer.md` (a fonte do contrato, não uma paráfrase), e a coluna Delivers ficou simétrica à do `sdd-qa` ("fix increments"): `40-review-r<N>.md` + `R<n>` increments.
- 2026-09-01 · `F3` · varredura da superfície **antes** de editar, para fechar a classe no arquivo e não só a linha citada: `grep -niE 'review.{0,60}fix|fix.{0,60}review'` sobre `README.md`, `docs/pipeline.md`, `docs/failure-modes.md` e `config/schema.md` devolve `README.md:152` como a única linha viva prometendo o contrato velho — as demais já descrevem o desenho novo (`pipeline.md:175` "The round finds; the EXEC phase fixes", `failure-modes.md:433` o `REVIEW-EDITED-CODE`).
- 2026-09-01 · `F3` · **achado deixado por consertar, de propósito.** Os dois diagramas de ordem canônica — `README.md:12` e `docs/pipeline.md:26` — desenham `QA ⇄ EXEC` e `REVIEW` em linha reta, enquanto o laço `REVIEW ⇄ EXEC` é hoje o espelho exato do de QA (achado vira `R<n>` `pending`, `gate_EXEC` volta a reprovar). Consertar só o do README poria o README **à frente** do doc de referência; é decisão da fase DOCS e foi para `20-handoff-exec.md § Achados fora de escopo`, junto com a observação de que nenhum instrumento mede prosa de contrato fora de `templates/`.
- 2026-09-01 · `F3` · nenhum sensor mudou nesta linha: 857 asserções antes e depois, nenhum piso se move, catálogo de mutação em 222. `README.md` **não** está na chave do carimbo de mutação (`bin/ tests/ templates/ config/`), mas o carimbo segue morto desde `88432ee` e o `F4` ainda vai tocar `bin/` e `tests/` — quem re-emite continua sendo a fase DOCS. Nada foi escrito no `TODO.md` pelo mesmo motivo das sessões anteriores (`tests/health-baseline.txt` está na chave).
- 2026-09-01 · `I1` · o carimbo de mutação (`.sdd/logs/mutation-stamp`) está **morto** a partir deste commit: `bin/ tests/` mudaram. Quem re-emite é a fase DOCS, depois do último commit de código (`01-plano.md § Para a fase DOCS`). Catálogo passa de 218 para 220 mutantes; a suíte rápida vai a 845 asserções.

> **Toda vez que um humano precisou entrar na linha** — um `sdd retry`, um conserto à mão, um
> `BLOCKED` assumido — sai uma linha com o marcador `intervention:`. É a **narrativa** do que o
> humano fez. O **número** de intervenções o `sdd autonomy --by-mission` lê do ledger, em
> `launch(es)` (`run_id` distintos — cada `sdd run`/`sdd retry`), e imprime estas notas ao lado
> como `intervention note(s)`. Medido em 2026-08-28: a missão de três lançamentos tinha zero
> notas — o contador não pode depender de alguém lembrar de escrever; a narrativa, sim, e é a
> única fonte que diz o que o humano *fez*.
>
> ⚠️ O marcador é **inglês e minúsculo**, como `pending`/`done`/`blocked`: é contrato, não prosa.
> O texto depois dos dois-pontos vai no idioma do `OUTPUT_LANG`, como o resto deste arquivo.
> Só conta quando abre a linha — `intervention` no meio de uma frase é prosa e não é contado.
>
> O exemplo abaixo mora **dentro** desta citação de propósito: o `>` quebra o casamento com
> `^[[:space:]]*-`, e sem ele o exemplo era contado verbatim — todo checkpoint recém-instanciado
> nascia devendo uma intervenção fantasma ao instrumento que mede autonomia. Copie a forma para
> fora da citação ao registrar uma intervenção de verdade.
>
> - intervention: <o que o humano teve de fazer> — <fase> — <custo, se houver>

## Incrementos de fix (QA)

> Escritos pelo `sdd-qa` quando um bug sanável é reprovado. Entram na mesma tabela acima com ID
> `F<n>`, e o Check obrigatoriamente inclui **regression test passa** + **re-walk da jornada
> impactada verde**. Bug que exige julgamento humano NÃO vira fix — vai para
> "Decisions for a Human" no handoff de QA.
>
> ⚠️ **Nesta missão a fase REVIEW passa a fazer o mesmo** (é o objeto do I2): achado a consertar
> entra na tabela acima com ID `R<n>`, escrito pelo `sdd-reviewer`, e o `sdd-executor` o pega como
> qualquer linha `pending` — a REVIEW desta própria missão é a primeira a rodar nesse contrato.
