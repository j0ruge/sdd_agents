---
missao: 20260901-o-revisor-so-acha
fase: PR
status: done
pr_url: https://github.com/j0ruge/sdd_agents/pull/34
data: 2026-09-02 09:30
gate: "gh pr view https://github.com/j0ruge/sdd_agents/pull/34 --json url,state → {\"state\":\"OPEN\",\"url\":\"https://github.com/j0ruge/sdd_agents/pull/34\"}"
---

# PR — O revisor só acha, e o executor conserta

> Escrito no fim da fase. É o `pr_url` acima, confirmado por `gh`, que o `gate_PR` cobra — nunca o
> texto deste arquivo.

## TL;DR

Branch empurrada (`git push -u origin feat/o-revisor-so-acha`, `[new branch]`, sem remoto anterior)
e PR aberto: **https://github.com/j0ruge/sdd_agents/pull/34**. Antes de empurrar: árvore limpa,
`./bin/sdd preflight` ok (0 vermelhos, 7 agentes do kit em dia), `tests/run-all.sh` verde
(**866** `ok`, 0 FAIL) e o carimbo de mutação **válido** — a chave recomputada nesta sessão sobre
`bin tests templates config` (`e62cf049f5e032f3acc635dd5e82190c`) bate byte a byte com
`.sdd/logs/mutation-stamp`, escrito pela fase DOCS (`227 caught of 227`, 0 gaps) e confirmado sem
nenhum commit de código entre aquela escrita e este push (os três commits depois de `bd71f48` só
tocam `README.md`, `docs/` e `CONTEXT.md`, fora da chave). Nenhum código foi tocado por esta sessão.

## Checks antes do push

- **Árvore limpa:** `git status --porcelain` vazio.
- **Branch correta:** `feat/o-revisor-so-acha`, diferente de `DEFAULT_BRANCH` (`main`).
- **Suíte (`TEST_CMD`):** `bash tests/run-all.sh` → rc 0, `suite green`, **866** `ok`, **0** `FAIL`.
- **`E2E_CMD`:** vazio — não existe e não roda; o kit não tem interface.
- **Carimbo de mutação:** `mutation_stamp_key` recomputado nesta sessão sobre `bin tests templates
  config` = `e62cf049f5e032f3acc635dd5e82190c`, idêntico ao conteúdo de
  `.sdd/logs/mutation-stamp` — a chave que a fase DOCS gravou depois do último commit de código
  (`bd71f48`) continua válida, porque nenhum commit desde então tocou as quatro pastas da chave.
  `gate_PR` cobra exatamente essa comparação (`bin/sdd:1369-1388`).
- **Conflito com a base:** nenhum. Branch sem remoto anterior — `git push -u` criou
  `origin/feat/o-revisor-so-acha` do zero, sem rebase e sem merge.

## O PR

- **Título:** `feat(review): o revisor só acha, e o executor conserta`
- **URL:** https://github.com/j0ruge/sdd_agents/pull/34
- **Corpo:** montado a partir de `templates/pr-body.md`, preenchido só com o que está em
  `00-missao.md`, `01-plano.md`, `checkpoint.md`, `20-handoff-exec.md`, `30-handoff-qa.md`,
  `40-review-r1.md`–`r4.md` e `45-docs.md` — nada inventado. Traz: o que mudou em linguagem de
  produto, os comandos de verificação na ordem certa, a tabela de evidências por fase com link para
  cada handoff, os sensores novos que passam a rodar em `tests/run-all.sh`, a união das pendências
  humanas das cinco fases (janela cega do runner velho, constante da régua de lote, abertura da
  janela 3, os dois combinados de pós-pipeline do grill, e a semente de sensor que a r3 deixou),
  destaques dos 17 achados que foram para o `TODO.md`, e os riscos honestos — inclusive que a
  métrica M2 desta própria missão não fecha e a M1 só é julgada depois do merge.

## Artefatos

| Arquivo | O que contém |
|---|---|
| `docs/handoffs/20260901-o-revisor-so-acha/50-pr.md` | este handoff |

## Pendências / Decisions for a Human

Todas foram levadas para o corpo do PR (seção "Pendências (Decisions for a Human)"), não para esta
seção — é o ponto em que o humano já as vê ao abrir o link. Resumo:

1. Conserto durável da janela cega do runner velho (comparar hash de `bin/sdd` na entrada, ou citar
   `.sdd/logs/` na métrica) — `40-review-r1.md § Pendências`.
2. A constante da régua de lote de achados MEDIUM/LOW está errada em 3–5× — `45-docs.md § Pendências`.
3. Quando abrir/fechar a janela 3 de medição (no sha do merge deste PR, padrão D19).
4. Os dois protocolos combinados no planejamento para depois do pipeline/merge (`qa-report`/
   `/codereview:codereview` até A; retrofit lean da lição de bash na skill `graphify` do
   `sales_quote`) — `00-missao.md § Pendências para o humano`.
5. Se as três famílias de defeito sem instrumento que a r3 apontou merecem sensor novo em
   `tests/check-pipefail.sh` — `40-review-r3.md § Pendências`, candidato de missão futura.

## Riscos e não-feitos

- Nada foi consertado nesta sessão — só verificado e publicado. Todo o código, os 14 incrementos de
  correção (`F1`–`F4`, `R1`–`R10`) e a documentação já estavam no `git log` ao início desta sessão.
- Esta sessão não rodou `./bin/sdd health` de novo: a chave do carimbo em disco já bate com o HEAD
  atual, e re-rodar o catálogo inteiro (15–50 min) só para confirmar o que a própria lógica de
  escrita do `sdd health` já garante seria o desperdício que o `CLAUDE.md` descreve.
- **A M2 desta missão não fecha** (por rodada melhorou 62%; pelo laço piorou, US$ 102,39 = 68% de
  US$ 150,42 contra um teto de US$ 40) e **a M1 — a métrica que decide se a missão valeu — só é
  julgada na janela 3**, depois do merge. Isso está no corpo do PR, não escondido aqui.
- `.sdd/logs/*.stream.jsonl`/`*.json` desta e das sessões anteriores não são artefato de missão —
  ficam em `.sdd/logs/`, fora do controle de versão, como sempre.

## Achados fora de escopo

Nenhum novo nesta sessão. Os 17 achados desta missão já estão no `TODO.md` (transportados pela fase
DOCS em `bd71f48`, catraca `todo-findings` 92 → 109 no mesmo commit) — destacados no corpo do PR.
