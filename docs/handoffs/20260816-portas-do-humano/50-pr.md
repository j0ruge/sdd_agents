---
missao: 20260816-portas-do-humano
fase: PR
status: done
pr_url: https://github.com/j0ruge/sdd_agents/pull/5
data: 2026-08-17 02:36
gate: "gh pr view https://github.com/j0ruge/sdd_agents/pull/5 --json url → ok (number 5, state OPEN)"
---

# PR — as portas de controle do humano

> Escrito no fim da fase. A próxima etapa é humana: o merge.

## Pré-checagens

- `git status --porcelain` → vazio (árvore limpa).
- `bash tests/run-all.sh` → rc 0, `suite green`, `score: 55 caught, 0 known gap(s), of 55`,
  479 asserções `ok`. `E2E_CMD=""` — o kit não tem interface, nada a rodar.
- `./bin/sdd health` → `kit healthy` nos 5 checks.
- Branch atual: `missao/20260816-portas-do-humano` (≠ `DEFAULT_BRANCH=main`).
- Os 4 itens da métrica do `00-missao.md` carregam `RESOLVIDO por <hash>` no `TODO.md`
  (`96a1f68`, `b3b8c2f`, `3ffa586`, `2510c3c`).

## Push

`git push -u origin missao/20260816-portas-do-humano` — sem conflito com `origin/main`
(`git fetch origin main` não trouxe commit novo; `main..HEAD` = 41 commits à frente, `HEAD..origin/main`
vazio).

## PR

`gh pr create --base main --head missao/20260816-portas-do-humano`, corpo montado a partir de
`templates/pr-body.md` com o conteúdo de `checkpoint.md`, `20-handoff-exec.md`, `30-handoff-qa.md`,
`40-review-r1.md`, `45-docs.md`, `TODO.md` e `KAIZEN_LOG.md` (nenhum dado inventado; a tabela
Antes/Depois e a atribuição dos 11 mutantes novos por commit vieram do `KAIZEN_LOG.md`).

- **URL:** https://github.com/j0ruge/sdd_agents/pull/5
- **Título:** `feat(runner): aprovar vira comando, e a branch declarada é honrada`
- **Base:** `main` · **Head:** `missao/20260816-portas-do-humano`
- Confirmado via `gh pr view … --json url,number,state,title` → `{"number":5,"state":"OPEN",…}`

## Notas

- A métrica "a classe SQ-97 morre" foi levada ao corpo do PR **com a ressalva** que a REVIEW
  pediu (`40-review-r1.md`, achado #4): morre no caminho **sem** `JIRA_ENABLED`, o único que esta
  missão andou.
- "Decisões para o humano" no PR é a união do que os handoffs registraram como fora de escopo e
  exigindo julgamento humano: o eixo do juiz / I13.4 (declarado fora de escopo no `00-missao.md`),
  a meta "suíte < 30s" da D7 (`CONTEXT.md` § 🚩, terceira medição consecutiva confirmando o desvio)
  e o par `BUDGET_PER_PHASE_USD`/chaves fantasma do `config/schema.md` (também fora de escopo no
  `00-missao.md`). Nenhum handoff desta missão registrou pendência própria — EXEC, QA e a REVIEW
  concordam em "nenhuma decisão humana pendente" para o que foi implementado.
- "Achados fora de escopo" no PR lista os 6 itens que a REVIEW e o EXEC abriram no `TODO.md` desta
  missão e que seguem abertos (não confundir com os 3 que a própria REVIEW já fechou com
  `RESOLVIDO por ad0c89d`).
