---
missao: 20260829-o-incremento-que-andou
fase: PR
status: done
pr_url: https://github.com/j0ruge/sdd_agents/pull/30
data: 2026-08-30 04:28
gate: "gh pr view https://github.com/j0ruge/sdd_agents/pull/30 --json url,state → {\"state\":\"OPEN\",\"url\":\"https://github.com/j0ruge/sdd_agents/pull/30\"}"
---

# PR — o incremento que andou

> Escrito no fim da fase. É o `pr_url` acima, confirmado por `gh`, que o `gate_PR` cobra — nunca o
> texto deste arquivo.

## TL;DR

Branch empurrada (`git push -u origin feat/o-incremento-que-andou`, fast-forward puro sobre o que
já estava em `origin`) e PR aberto: **https://github.com/j0ruge/sdd_agents/pull/30**. Antes de
empurrar: árvore limpa, `tests/run-all.sh` verde (**700** `ok`, 0 FAIL, medido nesta sessão sobre o
HEAD `019737e`) e o carimbo de mutação **válido** — a chave computada agora
(`de734a08ced4d1cf298915a830a4f815`) bate byte a byte com `.sdd/logs/mutation-stamp`, escrito às
04:18 por uma sessão de código anterior a esta, depois do commit que reancorou os 5 mutantes
quebrados (`d5d817c`, 03:55). Nenhum código foi tocado por esta sessão.

## As duas sessões de PR anteriores, e por que esta não repete o trabalho delas

Esta é a **terceira** sessão da fase PR desta missão. As duas primeiras (mesmo `session_id`
`60ce7ca8`, retomada) já estão no `git log`/`pipeline.log`:

1. A primeira (`PR-20260830-022527`, US$ 1,46, 411s) tentou rodar `./bin/sdd health` em background
   e encerrou o turno "esperando a notificação" — em `claude -p` isso encerra a sessão, e o health
   morreu junto. Achado de kit, registrado no `20-handoff-exec.md` § Achados fora de escopo (não no
   `TODO.md`: a catraca do backlog está dentro da chave do carimbo, e movê-la ali teria matado o
   próprio carimbo que a sessão estava tentando emitir).
2. A segunda (`PR-20260830-023218`, US$ 2,73, 82min) rodou o health em primeiro plano até o fim:
   `190 caught of 195`, 5 sobreviventes, todos `CATALOGUE-BROKEN` (âncora de `sed` que não casava
   mais o texto em `bin/sdd`, depois de edições do I1 e da r1 do REVIEW — não lógica frouxa).
   Escreveu `50-pr.md` com `status: blocked`, recusando corrigir o arnês de teste na própria fase de
   publicação — exatamente a regra deste agente ("Do not fix it — it is not your phase").

Entre essa sessão e esta, uma sessão de **código** (fora do `sdd run`, ver `Claude-Session:` no
`git log` do commit) fez o que o `50-pr.md` bloqueado pedia: reancorou os 5 mutantes
(`d5d817c`), documentou o porquê (`e34aa0f`) e completou o handoff de EXEC com os dois achados de
kit que a fase PR expôs (`019737e`). Esta sessão **confirmou** o conserto (chave do carimbo bate
com o tree atual, suíte verde) e seguiu para push + PR — não repetiu o `sdd health` de 15-50 min,
porque o carimbo já em disco é válido para o HEAD atual (nenhum commit desde `d5d817c` tocou
`bin/ tests/ templates/ config/`).

## Checks antes do push

- **Árvore limpa:** `git status --porcelain` vazio.
- **Branch correta:** `feat/o-incremento-que-andou`, diferente de `DEFAULT_BRANCH` (`main`).
- **Suíte (`TEST_CMD`):** `bash tests/run-all.sh` → rc 0, `suite green`, **700** `ok`
  (`grep -c '^  ok    '`), **0** `FAIL`.
- **`E2E_CMD`:** vazio — não existe e não roda; o kit não tem interface.
- **Carimbo de mutação:** `mutation_stamp_key` recomputado nesta sessão sobre `bin tests templates
  config` = `de734a08ced4d1cf298915a830a4f815`, idêntico ao conteúdo de
  `.sdd/logs/mutation-stamp`. `mutation_stamp_key` só é gravado quando o catálogo saiu
  **verde** e a chave não se moveu durante a corrida (`bin/sdd:3277-3295`) — a validade não é
  suposta, é a própria lógica de escrita do `sdd health`.
- **Conflito com a base:** nenhum. `origin/feat/o-incremento-que-andou` era ancestral direto do
  HEAD local (`git merge-base --is-ancestor` → verdadeiro); o `git push` foi fast-forward puro
  (`316f2a0..019737e`), sem rebase e sem merge.

## O PR

- **Título:** `feat(autonomy): o incremento que andou lê advanced, não churned`
- **URL:** https://github.com/j0ruge/sdd_agents/pull/30
- **Corpo:** montado a partir de `templates/pr-body.md`, preenchido com o que está em
  `00-missao.md`, `01-plano.md`, `checkpoint.md`, `20-handoff-exec.md`, `30-handoff-qa.md`,
  `40-review-r1.md` e `45-docs.md` — nada inventado. Traz: o que mudou (linguagem de produto), os
  comandos de verificação, a tabela de evidências por fase com link para cada handoff, os sensores
  novos que passam a rodar no CI, as pendências para o humano (união das quatro fases + a semente da
  missão seguinte), os 8 achados do `TODO.md` desta missão com âncora, e os riscos e não-feitos —
  inclusive os dois achados de kit ainda fora do `TODO.md` e o histórico das duas sessões de PR
  anteriores, para quem for revisar não precisar reconstruir a linha do tempo pelo `git log`.

## Artefatos

| Arquivo | O que contém |
|---|---|
| `docs/handoffs/20260829-o-incremento-que-andou/50-pr.md` | este handoff |

## Pendências / Decisions for a Human

Todas foram levadas para o corpo do PR (seção "Pendências (Decisions for a Human)"), não para
esta seção — é o ponto de decisão que o humano já vê ao abrir o link. Resumo:

1. Adotar o invariante do caminho histórico ou declarar a inferência atual (`TODO.md:649`).
2. O ledger global contaminado por missões de fixture dos próprios testes (`TODO.md:658`).
3. A próxima missão (custo do REVIEW) — semente desenhada em `01-plano.md`, para grilar com o
   humano.
4. Os dois achados de kit registrados no `20-handoff-exec.md` (health em background numa sessão
   headless; âncora morta de mutante só visível no catálogo inteiro) esperam um humano para
   transportá-los ao `TODO.md`, com a catraca do backlog no mesmo diff — não é decisão desta
   sessão mover essa catraca sozinha.

## Riscos e não-feitos

- Nada foi consertado nesta sessão — só verificado e publicado. O conserto do catálogo (`d5d817c`)
  e a nota de bloqueio anterior (`e34aa0f`) já estavam no `git log` ao início desta sessão.
- Esta sessão não rodou `./bin/sdd health` de novo: a chave do carimbo em disco já bate com o HEAD
  atual, e re-rodar o catálogo inteiro (15-50 min) só para confirmar o que a própria lógica de
  escrita do `sdd health` já garante seria o mesmo desperdício que o achado de kit #2 desta missão
  aponta (âncora morta só aparece no catálogo inteiro; validade do carimpo, não).
- `.sdd/logs/*.stream.jsonl`/`*.json` desta e das sessões de PR anteriores não são artefato da
  missão — ficam em `.sdd/logs/`, fora do controle de versão, como sempre.

## Achados fora de escopo

Nenhum novo. Os achados desta missão já estão registrados: 8 no `TODO.md` (pela r1 do REVIEW) e 2
no `20-handoff-exec.md` (achados de kit, aguardando transporte humano pós-merge — ver Pendências
acima).
