# Matriz de aceite reconciliada

| Id | Seção | Requisito (plano aprovado) | Evidência |
| --- | --- | --- | --- |
| 1 | 1. Pedido, autorização e contexto | Priorizar **confiabilidade**, preservando o pipeline atual. | Revisões das Tarefas 1 e 2 e a re-revisão integral aprovaram o código executável final; `sdd health` certificou 373/373 no estado estável `8875643`. |
| 2 | 1. Pedido, autorização e contexto | Entregar em **duas etapas**: primeiro três correções pontuais; depois exclusão mútua. | Os commits `7b0c2df`, `46a288e` e `9089493` da Tarefa 1 precedem a Tarefa 2. A decisão está em [`plano-aprovado.md`](plano-aprovado.md) §1, e a execução sequencial está no membro `execution/progress.md` do pacote local de evidências citado em [`entrega.md`](entrega.md). |
| 3 | 1. Pedido, autorização e contexto | Trabalhar em **checkout separado, baseado no HEAD analisado**, preservando o checkout original. | [`plano-aprovado.md`](plano-aprovado.md) §2 fixa a base `1c9146d` e o checkout separado; [`entrega.md`](entrega.md) registra o checkout usado e o ponto de controle final do original. |
| 4 | 1. Pedido, autorização e contexto | Quando o checkout estiver ocupado, **recusar imediatamente e informar o proprietário**, sem fila de espera. | `tests/check-coordination.sh` exige retorno 75 imediato, `CHECKOUT-BUSY` e diagnóstico do proprietário; a revisão da implementação foi concluída. |
| 5 | 2. Estado verificado e preparação | HEAD: `1c9146d18d2bb73ada7283d5b9e70214249cf397`. | A base completa está preservada em [`plano-aprovado.md`](plano-aprovado.md) §2 e é repetida no estado inicial de [`entrega.md`](entrega.md). |
| 6 | 2. Estado verificado e preparação | Branch: `feat/a-excecao-do-chapeu-e-o-genero-diferido`. | O checkout original continuava nessa ramificação no ponto de controle documental final registrado em [`entrega.md`](entrega.md). |
| 7 | 2. Estado verificado e preparação | Única alteração indicada por `git status --short`: `?? AGENTS.md`. | O checkout original continuava com apenas `?? AGENTS.md`; o objeto Git permanecia `c04c9e9e56f09ad0301bcb7387ecc282144bc053`. |
| 8 | 2. Estado verificado e preparação | A configuração deste projeto usa `DEFAULT_BRANCH="main"`. Não havia `develop` nas referências locais ou remotas disponíveis. | O plano aprovado preserva a configuração específica `main`; nenhuma integração foi solicitada. |
| 9 | 2. Estado verificado e preparação | A suíte `bash tests/run-all.sh` terminou com `suite green` fora do sandbox. | A nova linha de base em `/tmp/sdd-reliability-baseline-20260918.log` terminou com retorno 0 e `suite green`. |
| 10 | 2. Estado verificado e preparação | A primeira execução restrita apresentou falhas decorrentes de sockets locais bloqueados e escrita proibida em `/var/tmp`; não atribuir essas falhas ao código. | Contexto histórico preservado no plano aprovado; a linha de base irrestrita passou, sem atribuir ao código as falhas anteriores do ambiente restrito. |
| 11 | 2. Estado verificado e preparação | Superfície do kit, código, comentários e testes em inglês. | `tests/check-lang.sh` passou na Tarefa 1, na Tarefa 2 e no conserto de `SIGINT`, com 51 caminhos na superfície final. |
| 12 | 2. Estado verificado e preparação | Artefatos desta missão em português, conforme `OUTPUT_LANG=pt-BR`. | Entrega durável, plano preservado, matriz, revisões e entrada KAIZEN estão em português. |
| 13 | 2. Estado verificado e preparação | TDD: escrever o Check e a regressão antes da correção. | Relatórios preservam RED/GREEN das duas etapas, dos três achados F1–F3, do complemento de `--spec` e do ambiente de teste que matou o sobrevivente legado. |
| 14 | 2. Estado verificado e preparação | Alterações pequenas; nenhuma refatoração geral do runner. | Revisões aprovaram as diferenças focadas até `8875643`; nenhuma quebra crítica, importante ou menor ficou aberta. |
| 15 | 2. Estado verificado e preparação | Preservar a última linha protegida de entrada de `bin/sdd`. | Suíte final e controle da recertificação passaram; certificação 373/373 terminou com árvore e chave estáveis. |
| 16 | 2. Estado verificado e preparação | Atualizar documentação junto das alterações de comportamento. | As Tarefas 1 e 2 atualizaram fluxo, modos de falha, esquema/requisitos e anatomia do agente junto das mudanças de comportamento. |
| 17 | 2. Estado verificado e preparação | Se agentes forem alterados, sincronizar os espelhos pelo comando `sdd install --force`. | Não aplicável: nenhum `agents/*.md` mudou no intervalo final. |
| 18 | 2. Estado verificado e preparação | Registrar achados fora de escopo conforme a régua de admissão e o formato do `TODO.md`. | Nenhum achado admitido ficou sem rota; nenhum item resolvido foi removido. Os dois incidentes de processo estão preservados na entrega e no registro de revisões. |
| 19 | 2. Estado verificado e preparação | Suíte: `/tmp/sdd-audit-suite-unrestricted-20260918.log`. | Evidência opcional da sessão anterior, substituída para implementação pela nova linha de base e por regressões determinísticas. |
| 20 | 2. Estado verificado e preparação | Fixtures: `/tmp/sdd-audit-probes-845fwrdg`. | Ambientes de teste opcionais da sessão anterior; os sensores novos reproduzem o comportamento sem depender desses arquivos. |
| 21 | Integridade do plano na troca de branch | Comparar os bytes de `00-missao.md` e `01-plano.md` com os da branch de destino antes do checkout. | F2 corrigido em `14b09a7` com resumos dos bytes crus via `--no-filters`; cinco falhas RED, sensor GREEN, revisão aprovada e mutantes capturados na certificação final. |
| 22 | Integridade do plano na troca de branch | Ausência ou divergência deve impedir a troca de branch, a escrita de intervenção e a abertura de sessão. | As regressões distinguem recusa antes do checkout e deriva posterior nos dois artefatos, inclusive filtro semântico e CRLF/LF; resultado final 373/373. |
| 23 | Integridade do plano na troca de branch | Revalidar os dois arquivos após o checkout, antes de continuar. | As leituras pós-checkout também usam bytes crus; mutante de revalidação foi detectado na recertificação. |
| 24 | Integridade do plano na troca de branch | Informar claramente os arquivos divergentes e as branches envolvidas. | A diferença revisada da Tarefa 1 nomeia origem, destino e cada arquivo divergente. |
| 25 | Integridade do plano na troca de branch | Preservar os comportamentos existentes para branch ausente, placeholder, branch atual e criação de branch nova. | Controles para ramificação ausente/atual, marcador de posição, ramificação nova e `dry-run` ficaram verdes. |
| 26 | Integridade do plano na troca de branch | Não exigir igualdade de checkpoint, notas ou handoffs: eles representam progresso que pode diferir entre branches. | O controle de plano idêntico com ponto de controle divergente passou no RED e no GREEN de `tests/check-gates.sh`. |
| 27 | Orçamento positivo menor que US$ 1 | Usar comparação numérica para reconhecer zero. | `46a288e`; comparação numérica revisada e aprovada na Tarefa 1. |
| 28 | Orçamento positivo menor que US$ 1 | Somente zero desabilita o teto, incluindo representações equivalentes. | Controles de zero e zero decimal em `tests/check-autonomy.sh`; suíte completa da Tarefa 1 verde. |
| 29 | Orçamento positivo menor que US$ 1 | Qualquer valor positivo deve ser comparado com o gasto acumulado. | `tests/check-autonomy.sh` cobre tetos fracionários e inteiros abaixo/igual/acima; o RED abriu duas sessões indevidas e o GREEN recusou. |
| 30 | Orçamento positivo menor que US$ 1 | Preservar validação existente, `--budget-override`, registro de intervenção e comportamento da projeção. | `tests/check-autonomy.sh`, `tests/check-dry-run.sh` e suíte completa ficaram verdes; a sobreposição e a intervenção foram preservadas na revisão. |
| 31 | Registro durável antes da notificação | Gravar o evento no ledger antes de chamar `ON_ESCALATION_CMD`. | `9089493`; o sensor comportamental lê `ledger=seen` de dentro do comando de notificação. |
| 32 | Registro durável antes da notificação | Limitar o hook a cinco segundos, com encerramento forçado após mais um segundo. | A Tarefa 2 supervisiona somente a árvore do comando de notificação; a regressão de filho escapado passou com orçamento 5+1 compartilhado com GNU `timeout`. |
| 33 | Registro durável antes da notificação | Preservar stdin fechado, variáveis de contexto e ausência de execução no dry-run. | `tests/check-autonomy.sh` das Tarefas 1 e 2 ficou GREEN; regressões preservam a entrada padrão e projeções silenciosas. |
| 34 | Registro durável antes da notificação | Falha ou timeout gera aviso e preserva o retorno original da escalada. | Controles de comando de notificação falho/travado preservam retorno 3; a árvore escapada termina dentro do prazo. |
| 35 | Registro durável antes da notificação | Preservar o diagnóstico existente para falha de escrita no ledger; não anunciar persistência que não aconteceu. | A revisão da Tarefa 1 confirmou a preservação do diagnóstico de falha da escrita durável. |
| 36 | Registro durável antes da notificação | Se faltar a ferramenta necessária ao timeout, avisar e não executar o hook sem limite. | `tests/check-autonomy.sh` e suíte final verdes; mutantes do comando de notificação e do limite de tempo foram capturados na certificação 373/373. |
| 37 | Posse e admissão | Implementar exclusão mútua com `flock`, por checkout físico. | Sensor final 154/154, suíte `d12ace4` verde e certificação 373/373 no conteúdo final. |
| 38 | Posse e admissão | Caminhos por symlink para o mesmo checkout compartilham a identidade; worktrees independentes recebem locks distintos. | Controles de ligação simbólica/árvore de trabalho, raiz de ADR e fronteira de especificação ficaram verdes; mutantes correspondentes foram capturados. |
| 39 | Posse e admissão | Centralizar a política de admissão, evitando guardas divergentes espalhadas pelos comandos. | Analisador compartilhado de ADR e despacho coordenado revisados; sensor final e certificação verdes. |
| 40 | Posse e admissão | Adquirir a posse antes de trocar branch, escrever artefatos, executar gates com efeitos ou abrir sessões. | Regressões exigem ausência de efeitos antes da admissão; controle 154/154 e catálogo 373/373. |
| 41 | Posse e admissão | Cobrir `run`, `retry`, `close`, `kaizen` com execução, `approve`, `install`, `adr new` e `preflight`. | Matriz de comandos coberta no sensor; `adr new --repo/--spec` ganhou fronteira física revisada; certificação final verde. |
| 42 | Posse e admissão | Cobrir também comandos que executam gates ou certificam conteúdo, incluindo `status` completo, `phase`, `why` e `health`. | A matriz de `tests/check-coordination.sh` cobre `status` completo, `phase`, `why` e `health`; consultas somente leitura permanecem disponíveis. |
| 43 | Posse e admissão | No `health`, proteger o checkout efetivamente medido, que pode diferir do diretório de onde o comando foi chamado. | A disputa do kit medido por `health` está coberta; o vínculo final de raiz em `cmd_health` deixou `tests/check-health.sh` GREEN com 36 capturas. |
| 44 | Posse e admissão | Incluir a entrada alternativa `sdd-link-agents`, pois ela altera índice Git, agentes instalados e `.gitignore`. | Entrada coordenada visível na ajuda em `8875643`; mutante do vinculador capturado na recertificação. |
| 45 | Contrato quando ocupado | Recusar imediatamente com retorno **75** e marcador **`CHECKOUT-BUSY`**. | Casos ocupados exigem retorno 75 e marcador; ficaram verdes no sensor 154/154, e os mutantes foram mortos. |
| 46 | Contrato quando ocupado | Informar checkout, processo proprietário, comando, missão e início. | Diagnóstico do proprietário coberto e mutante `COORD_owner_hidden` capturado. |
| 47 | Contrato quando ocupado | Não abrir sessão, alterar branch, escrever checkpoint ou executar hook de escalada. | Testemunhas de efeitos permanecem vazias na recusa; o catálogo final capturou sabotagens de admissão. |
| 48 | Contrato quando ocupado | Não registrar a recusa como sessão ou bloqueio de missão: a execução não foi admitida. | A admissão precede configuração, diário e comando de notificação; sensor final e certificação verdes. |
| 49 | Metadados, consultas e reentrada | Manter metadados operacionais fora dos arquivos versionados: checkout, proprietário, identidade do processo, início, comando, missão e identificador da execução. | Diretório de metadados por árvore de trabalho no Git; a revisão conferiu processo, instante de início, inicialização e identidade de execução sem encontrar problema. |
| 50 | Metadados, consultas e reentrada | O lock do sistema operacional decide a posse; o arquivo de metadados serve para diagnóstico. | Casos de trava somente no núcleo e metadados antigos/malformados passam; JSON não decide admissão. |
| 51 | Metadados, consultas e reentrada | Manter disponíveis consultas sem gates, como `status --no-gates`, `boot`, `census`, `autonomy`, `kaizen --series`, `adr check`, ajuda e versão. | A matriz de consultas passa durante posse ativa. |
| 52 | Metadados, consultas e reentrada | Exibir o proprietário ativo em `status --no-gates`. | Testemunha em `status --no-gates`; `COORD_owner_hidden` foi capturado. |
| 53 | Metadados, consultas e reentrada | Projeções que executam `TEST_CMD` também precisam de exclusão. | Projeções com verificações estão na matriz de admissão protegida; testes relevantes de `dry-run` passam. |
| 54 | Metadados, consultas e reentrada | Permitir auxiliares legítimos chamados pela execução proprietária, como `sdd health`, `sdd install --force` e criação de ADR. | Reentrada legítima e raiz física de ADR e `health` cobertas; sensor 154/154 e recertificação 373/373. |
| 55 | Metadados, consultas e reentrada | Verificar ancestralidade e identidade do proprietário para essa reentrada; uma variável de ambiente isolada não constitui autorização. | Regressões de ambiente forjado/copiado e identidades antigas de proprietário, trabalhador e supervisor passam. |
| 56 | Metadados, consultas e reentrada | Recusar abertura recursiva de outro pipeline no mesmo checkout. | Matriz de recusa recursiva para `run`/`retry`/`close`/`kaizen`; `COORD_recursive_pipeline` foi capturado. |
| 57 | Metadados, consultas e reentrada | Auxiliares não devem sobrescrever nem liberar a posse do processo externo. | Metadados do auxiliar reentrante e disputa ainda ativa passam; a revisão não encontrou problema restante. |
| 58 | Recuperação e limites | Supervisionar os processos da execução para manter exclusividade enquanto seus filhos ainda puderem escrever. | Testemunhas de descendente vivo cobrem descritor fechado, `setsid`, bifurcação dupla e escrita final antes da liberação. |
| 59 | Recuperação e limites | Tratar conclusão normal, erro, sinais e morte do proprietário com filho ainda ativo. | F3 corrigido em `f11bf27`: pidfd para processo em primeiro plano, `setsid` e filho de outra thread; testemunhas de tratador, barreira, ocupação, retorno e recuperação, 154/154 e revisão aprovada. |
| 60 | Recuperação e limites | Recuperar automaticamente após o encerramento dos processos. | Todos os regimes incluem recuperação após o último descendente; sensor final 154/154. |
| 61 | Recuperação e limites | Não apagar ou recriar o arquivo de lock durante uma disputa. | Um caminho persistente de trava do núcleo; disputa somente pelo núcleo e corrupção de metadados passam; revisão sem problema restante. |
| 62 | Recuperação e limites | Metadados antigos não podem causar bloqueio permanente nem autorizar dois proprietários. | Controles de recuperação com metadados antigos e recusa de identidade de processo antiga passam. |
| 63 | Recuperação e limites | Verificar e documentar as dependências antes de sessões pagas. | Linux 5.3+, procfs, Python 3.9+ e chamadas de sistema pidfd são verificadas antes de configuração/sessão; documentação e controles verdes. |
| 64 | Recuperação e limites | Preservar gates, guardas de ferramentas e caminhos e estado técnico derivado dos artefatos. | Suíte final `d12ace4` com retorno 0 e 154/154; recertificação em `8875643` manteve HEAD, estado e chave estáveis, sem deriva e com 373/373. |
| 65 | 5. Testes, documentação e entrega | Acrescentar as regressões da primeira etapa aos sensores existentes. | A Tarefa 1 acrescentou regressões em `tests/check-gates.sh`, `tests/check-autonomy.sh` e sete entradas no catálogo. |
| 66 | 5. Testes, documentação e entrega | Criar `tests/check-coordination.sh` com fixtures, sincronização determinística e processos concorrentes; usar limites de tempo para evitar testes pendurados. | O sensor novo usa ambientes de teste temporários isolados, substitutos de CLIs externas que falham de modo fechado, barreiras determinísticas e esperas limitadas. |
| 67 | 5. Testes, documentação e entrega | Cobrir disputa entre comandos diferentes, mesma missão e missões diferentes, symlinks, worktrees independentes, consultas durante execução, reentrada legítima e tentativas de bypass. | Sensor final 154/154 cobre comandos, missões, ligações simbólicas, árvores de trabalho, consultas, reentrada, tentativas de desvio e fronteiras de ADR/especificação. |
| 68 | 5. Testes, documentação e entrega | Cobrir recuperação após conclusão, erro, sinais e queda com filho sobrevivente. | Famílias de regressão de morte do proprietário/trabalhador e vida de descendentes passam. |
| 69 | 5. Testes, documentação e entrega | Verificar ausência de efeitos na invocação recusada, além do retorno e da mensagem. | Testemunhas da recusa exigem ausência de efeitos em ramificação, artefato, diário, comando de notificação e sessão. |
| 70 | 5. Testes, documentação e entrega | Integrar o sensor à suíte, ao catálogo de mutação e aos pisos de cobertura pertinentes. | Integração final: 16 sensores e 373 definições/entradas; suíte, pisos, controle original e controle sob 16 trabalhadores verdes. |
| 71 | 5. Testes, documentação e entrega | Exigir que cada mutante altere realmente a propriedade que pretende sabotar e seja detectado pelo motivo correto. | Recertificação: 373 resultados, todos com retorno 1 e falhas nomeadas; zero log ausente e zero caso sem falha nomeada. Os 27 novos e o sobrevivente antigo foram auditados; quatro diagnósticos incidentais têm detecção independente documentada. |
| 72 | 5. Testes, documentação e entrega | Executar sensores afetados e suíte completa em cada etapa. | RED/GREEN focados preservados; suíte final `d12ace4` com retorno 0, coordenação 154/154; controles da recertificação também verdes. |
| 73 | 5. Testes, documentação e entrega | Rodar a certificação completa de mutação depois de estabilizar as alterações e eventuais correções da revisão. Atualizações posteriores que invalidem o carimbo exigem nova certificação. | `SDD_MUTATION_JOBS=16 ./bin/sdd health` retornou 0: `373 caught, 0 known gap(s), of 373`, 6707.956 s; carimbo `d15d55d80f67c0b6f77ee8f3a2f32796`, igual à chave antes/depois, sem deriva. |
| 74 | 5. Testes, documentação e entrega | Checkout, branch e commits produzidos. | A entrega lista `7b0c2df`, `46a288e`, `9089493`, `c4ecb31`, `b7e012e`, `14b09a7`, `b76dfb5`, `f11bf27`, `d12ace4`, `d159a9d`, `8875643` e os commits documentais. |
| 75 | 5. Testes, documentação e entrega | Falhas corrigidas e comportamentos resultantes. | A entrega cobre plano em bytes crus, orçamento fracionário, registro/comando de notificação, proprietário único, raiz física de ADR/especificação, pidfds e ambiente de teste da guarda de opção. |
| 76 | 5. Testes, documentação e entrega | Testes executados e resultado da certificação. | Suíte final com retorno 0 e 154/154; certificação final com retorno 0, 373/373, `kit healthy`, arquivo de 374 logs de catálogo, 373 retornos e controle de carga. |
| 77 | 5. Testes, documentação e entrega | Limitações ou pendências efetivamente encontradas. | A entrega preserva requisitos de Linux, pidfd e Python, limites externos/ininterruptíveis, os dois incidentes e o custo desconhecido do Claude. Nenhuma correção funcional ou certificação está pendente; a revisão de código do usuário é o próximo passo. |
